<?xml version="1.0" encoding="UTF-8"?><unload unload_date="2026-09-04 12:19:23">
<sys_remote_update_set action="INSERT_OR_UPDATE">
<application display_value="Global">global</application>
<application_name>Global</application_name>
<application_scope>global</application_scope>
<application_version/>
<collisions/>
<commit_date/>
<deleted/>
<description>Qualys CI lookup rules (USEM custom chain, orders 175 to 850) rewritten so that any reader can follow them. Each rule script opens with its purpose, the sample Qualys Host Detection payload it handles, why the rule sits at its order and which hosts reach it, and the list of its stages. The code is split into numbered stages that state what happens, why the stage exists and what the sample data looks like, and every code line is followed by the data it produces. Result sets are no longer capped with setLimit: a rule accepts a CI only when exactly one candidate remains, or, for the tie-break rules, when the scanned IP confirms one of several candidates. Matching behaviour is otherwise unchanged.</description>
<inserted/>
<name>SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1</name>
<origin_sys_id/>
<parent display_value=""/>
<release_date/>
<remote_base_update_set display_value=""/>
<remote_parent_id/>
<remote_sys_id>615a913e930b8710e3aef0aefaba1057</remote_sys_id>
<state>loaded</state>
<summary/>
<sys_class_name>sys_remote_update_set</sys_class_name>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>191fd5f2938b8710e3aef0aefaba10bf</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<update_set display_value=""/>
<update_source display_value=""/>
<updated/>
</sys_remote_update_set>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_93ff4d7e936a4310e3aef0aefaba1064</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Unique short hostname across cmdb_ci_hardware; rejects owners whose class contradicts the scanned OS.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM Hostname Hardware Match&lt;/name&gt;&lt;order&gt;410&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;3&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 410 - USEM Hostname Hardware Match
   =================================================================================================
   PURPOSE
   Match by the short hostname anywhere in the hardware tree, with a sanity check that the CI found
   does not contradict the scanned OS. This is the second short-name stage, for CIs classed
   generically (Server, Computer, UNIX Server) or differently from the OS.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "80217765",
     "IP": "171.150.219.123",
     "TRACKING_METHOD": "IP",
     "OS": "AIX 7.3 TL3",
     "DNS": "va2ausapabw0.bankofamerica.com",
     "QG_HOSTID": "6337e0dc-007d-0002-c47d-005056a4fcd5"
   }

   sourceValue   = the DNS field of that record -&gt; "va2ausapabw0.bankofamerica.com"
   sourcePayload = the whole record above; the rule also reads the OS from it
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the one hardware CI named "va2ausapabw0"; accepted when it is an
   AIX Server or a generically classed Server, rejected when it is, say, a Windows Server (an AIX
   scan cannot belong to a Windows machine).

   WHY THIS RULE SITS AT ORDER 410
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 400 required the short name to be unique inside the class the OS implies.
   - Reaches it: hosts whose CI is classed generically (Server, Computer, UNIX Server) or whose OS
                 gave no class, so 400 found nothing.
   - After it  : 450 (CI named with the full FQDN) and then the IP rules (700 and above) for hosts
                 without a usable name.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a DNS name
    2. Clean the DNS name and take the short hostname
    3. Read the list of CI classes that must never be matched
    4. Work out the CMDB class from the scanned operating system (a preference, not a requirement)
    5. List the classes that never contradict the scan
    6. Search the whole hardware tree for the short name
    7. Require exactly one owner
    8. Reject an owner whose class contradicts the scanned OS
    9. Return the owner
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a DNS name
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "va2ausapabw0.bankofamerica.com" -&gt; not empty -&gt; the rule
    //                carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the DNS name and take the short hostname
    // What happens : '' + sourceValue turns the value into plain text, trim() removes blanks at
    //                both ends, toLowerCase() lowers it, and split(".")[0] keeps the text before
    //                the first dot.
    // Why          : CIs without domain information carry only the short name, so the short name is
    //                what has to be compared. A bare label with no dot works as well: split gives
    //                the whole text back.
    // Sample       : full = "va2ausapabw0.bankofamerica.com"; split(".") = ["va2ausapabw0",
    //                "bankofamerica", "com"]; host = "va2ausapabw0".
    // ---------------------------------------------------------------------------------------------
    var full = ('' + sourceValue).trim().toLowerCase();
    // -&gt; full = "va2ausapabw0.bankofamerica.com".
    var host = full.split('.')[0];
    // -&gt; host = "va2ausapabw0".
    if (!host)
        return null;
    // -&gt; host is empty only for a name that starts with a dot -&gt; decline; "va2ausapabw0" -&gt; carry
    //    on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Work out the CMDB class from the scanned operating system
    // What happens : the helper classFor() reads the operating system text Qualys reports and
    //                returns the CMDB class such a machine is stored in. It lowers the text, gives
    //                up when the text is an unauthenticated multi-guess fingerprint (three or more
    //                guesses separated by "/"), then looks for key words in a fixed order and
    //                returns an empty string when nothing fits. Searching a class also searches
    //                every class beneath it, so cmdb_ci_linux_server covers all Linux flavour
    //                classes.
    // Why          : evidence is only trusted when it lands in a class that agrees with the scanned
    //                OS: a Red Hat host must resolve to a Linux Server CI, never to a Windows
    //                Server that happens to carry the same value.
    // Sample       : classFor("AIX 7.3 TL3"): s = "aix 7.3 tl3"; s.split("/") has 1 part; "esx" and
    //                "windows" are not found; "aix" is found -&gt; returns "cmdb_ci_aix_server" (AIX
    //                Server).
    // ---------------------------------------------------------------------------------------------
    function classFor(os) {
        if (!os) return '';
        // -&gt; no OS text at all -&gt; "" (no class).
        var s = ('' + os).toLowerCase();
        // -&gt; s = the OS text in lower case, so that "Windows", "WINDOWS" and "windows" all compare
        //    the same.
        if (s.split('/').length &gt; 2) return '';
        // -&gt; "ubuntu / tiny core linux / linux 2.6.x / ibm asm / hp storeonce / f5 networks big-ip
        //    / cisco ios software" splits into 7 parts at the "/" characters -&gt; more than 2 -&gt; an
        //    unauthenticated guess list -&gt; "" (no class).
        if (s.indexOf('esx') != -1) return 'cmdb_ci_esx_server';
        // -&gt; "vmware esxi 7.0.3 build 24723872" contains "esx" -&gt; "cmdb_ci_esx_server" (ESX
        //    Server).
        if (s.indexOf('windows') != -1)
            return s.indexOf('server') != -1 ? 'cmdb_ci_win_server' : 'cmdb_ci_computer';
        // -&gt; "windows server 2016 standard 64 bit edition version 1607" -&gt; "cmdb_ci_win_server"
        //    (Windows Server); "windows 10 enterprise 64 bit edition version 22h2" has no "server"
        //    -&gt; "cmdb_ci_computer" (Computer, a workstation).
        if (s.indexOf('aix') != -1) return 'cmdb_ci_aix_server';
        // -&gt; "aix 7.3 tl3" -&gt; "cmdb_ci_aix_server" (AIX Server).
        if (s.indexOf('solaris') != -1 || s.indexOf('sunos') != -1) return 'cmdb_ci_solaris_server';
        // -&gt; "oracle solaris 11.4" -&gt; "cmdb_ci_solaris_server" (Solaris Server).
        if (s.indexOf('hp-ux') != -1) return 'cmdb_ci_hpux_server';
        // -&gt; "hp-ux b.11.31" -&gt; "cmdb_ci_hpux_server" (HPUX Server).
        if (s.indexOf('netapp') != -1 || s.indexOf('ontap') != -1) return 'cmdb_ci_storage_server';
        // -&gt; "netapp ontap 9.12.1" -&gt; "cmdb_ci_storage_server" (Storage Server).
        if (s.indexOf('printer') != -1 || s.indexOf('laserjet') != -1 || s.indexOf('jetdirect') != -1) return 'cmdb_ci_printer';
        // -&gt; "hp laserjet m507" -&gt; "cmdb_ci_printer" (Printer).
        if (s.indexOf('red hat') != -1 || s.indexOf('linux') != -1 || s.indexOf('centos') != -1 ||
            s.indexOf('ubuntu') != -1 || s.indexOf('suse') != -1 || s.indexOf('debian') != -1 ||
            s.indexOf('fedora') != -1 || s.indexOf('euleros') != -1 ||
            s.indexOf('oracle enterprise') != -1 || s.indexOf('amazon') != -1) return 'cmdb_ci_linux_server';
        // -&gt; "red hat enterprise linux 9.8", "suse linux enterprise server 15 sp5" and "amazon
        //    linux 2023" -&gt; "cmdb_ci_linux_server" (Linux Server).
        if (s.indexOf('nx-os') != -1 || s.indexOf('catos') != -1 || s.indexOf('cisco') != -1) return 'cmdb_ci_netgear';
        // -&gt; "cisco nx-os 9.3(8)" -&gt; "cmdb_ci_netgear" (Network Gear).
        return '';
        // -&gt; anything else, for example "unknown" -&gt; "" (no class).
    }

    var pref = classFor(sourcePayload.OS);
    // -&gt; pref = "cmdb_ci_aix_server" for the sample OS "AIX 7.3 TL3".
    // -&gt; unlike rule 400, an empty pref does not stop this rule: the search below runs without a
    //    class, and stage 8 simply has nothing to check.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - List the classes that never contradict the scan
    // What happens : lists the classes that say nothing about the operating system: Hardware,
    //                Computer, Server and UNIX Server. A CI stored in one of these never
    //                contradicts the scan.
    // Why          : many CIs are loaded into the generic Server class before discovery refines
    //                them; rejecting those would leave a large part of the estate unmatched for no
    //                good reason.
    // Sample       : generic["cmdb_ci_server"] = 1 (true), generic["cmdb_ci_win_server"] =
    //                undefined (false).
    // ---------------------------------------------------------------------------------------------
    var generic = {cmdb_ci_hardware: 1, cmdb_ci_computer: 1, cmdb_ci_server: 1,
        cmdb_ci_unix_server: 1};
    // -&gt; generic = a lookup table with the four class names; generic[cls] answers "is this class
    //    generic?".
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Search the whole hardware tree for the short name
    // What happens : opens a search on cmdb_ci_hardware, the parent class of every device class,
    //                keeps only CIs whose name equals the short hostname, and leaves out every
    //                class on the ignore list.
    // Why          : the class-scoped search of rule 400 found nothing, so the name is now searched
    //                everywhere; the safety comes from the two checks that follow.
    // Sample       : the search on cmdb_ci_hardware for name = "va2ausapabw0" finds the Server
    //                "va2ausapabw0" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c, class
    //                cmdb_ci_server).
    // ---------------------------------------------------------------------------------------------
    var gr = new GlideRecord('cmdb_ci_hardware');
    // -&gt; gr = a search on Hardware and every class beneath it.
    gr.addQuery('name', host);
    // -&gt; condition added: name = "va2ausapabw0" (case-insensitive).
    if (ignore)
        gr.addQuery('sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
    //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    gr.query();
    // -&gt; the search has run.
    // ---------------------------------------------------------------------------------------------
    // STAGE 7 - Require exactly one owner
    // What happens : reads the first CI, remembers its sys_id and its class, and declines when a
    //                second CI carries the same name.
    // Why          : two hardware CIs with one short name (a test and a production machine, a
    //                retired and a rebuilt one) can never be told apart by the name alone.
    // Sample       : one row -&gt; id = "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c", cls = "cmdb_ci_server".
    //                Two rows -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    if (!gr.next())
        return null;
    // -&gt; no CI has this name -&gt; decline; the host continues to rule 450.
    var id = gr.getUniqueValue();
    // -&gt; id = "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    var cls = '' + gr.getValue('sys_class_name');
    // -&gt; cls = "cmdb_ci_server" (the exact class of the CI found).
    if (gr.hasNext())
        return null;
    // -&gt; a second CI with the same name is waiting in the result -&gt; ambiguous -&gt; decline.
    // ---------------------------------------------------------------------------------------------
    // STAGE 8 - Reject an owner whose class contradicts the scanned OS
    // What happens : when the OS gave a class preference, checks that the CI found sits inside that
    //                class (or one of its sub-classes) or is generically classed; any other class
    //                is a contradiction and the rule declines.
    // Why          : a host scanned as AIX that resolves by name to a Windows Server CI is a
    //                namesake, not the same machine; linking the findings there would send AIX
    //                vulnerabilities to a Windows owner.
    // Sample       : pref = "cmdb_ci_aix_server"; chk.get("3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c") on
    //                the AIX Server class is false because the CI is a plain Server, but
    //                generic["cmdb_ci_server"] is true -&gt; accepted. For a Windows Server CI both
    //                checks fail -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    if (pref) {
        var chk = new GlideRecord(pref);
        // -&gt; chk = a search on the AIX Server class and its sub-classes.
        if (!(chk.isValid() &amp;&amp; chk.get(id)) &amp;&amp; !generic[cls])
            return null;
        // -&gt; chk.get(id) is true when the CI is an AIX Server; generic[cls] is true when it is
        //    generically classed; when neither holds the CI contradicts the scan -&gt; decline.
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 9 - Return the owner
    // ---------------------------------------------------------------------------------------------
    return id;
    // -&gt; the sys_id "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c" goes back to the framework, which links the
    //    vulnerable item to the CI and stops evaluating later rules.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;DNS&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:42:02&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;93ff4d7e936a4310e3aef0aefaba1064&lt;/sys_id&gt;&lt;sys_mod_count&gt;2&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:20&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-82472092</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>111fd5f2938b8710e3aef0aefaba10c2</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf2750000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM Hostname Hardware Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>b01fd5f2898b87101b0b4973d5330846</update_guid>
<update_guid_history>b01fd5f2898b87101b0b4973d5330846:-82472092,5e8a997ec40b87105ef5dfbae115c74c:1245545680</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_abff4d7e936a4310e3aef0aefaba1076</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;The single deliberately-late broad fallback: CI named with the full FQDN anywhere outside the ignored classes; unique owner required.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM FQDN Name Broad Match&lt;/name&gt;&lt;order&gt;850&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;3&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 850 - USEM FQDN Name Broad Match
   =================================================================================================
   PURPOSE
   The single, deliberately late, broad fallback: a CI named with the full FQDN anywhere in the CI
   table, including classes outside the hardware tree (for example a virtual machine instance or
   another logical CI). It still requires a unique owner outside the ignored classes.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "71973166",
     "IP": "164.91.209.12",
     "TRACKING_METHOD": "AGENT",
     "OS": "Red Hat Enterprise Linux 8.10",
     "DNS": "lva40bneehcs01.ecomm.devicenp.rpg",
     "QG_HOSTID": "633781ed-019b-0002-2f2f-0050569d20ff"
   }

   sourceValue   = the DNS field of that record -&gt; "lva40bneehcs01.ecomm.devicenp.rpg"
   sourcePayload = the whole record above
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the one CI in any class whose name is
   "lva40bneehcs01.ecomm.devicenp.rpg", for example a Virtual Machine Instance; declined when the
   name is shared.

   WHY THIS RULE SITS AT ORDER 850
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : every hardware-scoped rule (175 to 740) has declined for this host.
   - Reaches it: the rare CI named with the full FQDN that lives outside the hardware tree.
   - After it  : the out-of-box Qualys rules: 860 QUALYS HOST ID, 880 Cloud Resource Id, 900 FQDN,
                 920 NetBIOS, 940 DNS, and 950/960 IP which are inactive by default. Those are the
                 vendor's best-effort matching.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a DNS name
    2. Clean the DNS name and require a domain part
    3. Read the list of CI classes that must never be matched
    4. Search the whole CI table for a CI named with the full FQDN
    5. Decide: exactly one CI, or decline
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a DNS name
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "lva40bneehcs01.ecomm.devicenp.rpg" -&gt; not empty -&gt; the rule
    //                carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the DNS name and require a domain part
    // What happens : '' + sourceValue turns the value into plain text, trim() removes blanks at
    //                both ends and toLowerCase() lowers it. Then the rule requires at least one dot
    //                in the name.
    // Why          : DNS names are case-insensitive and CMDB values are stored in mixed case, so
    //                both sides are compared in lower case. A name without a dot is a bare
    //                hostname, which the hostname rules (400 and 410) look after.
    // Sample       : fqdn = "lva40bneehcs01.ecomm.devicenp.rpg"; indexOf(".") finds a dot -&gt; carry
    //                on. A bare label "lva40bneehcs01" has no dot -&gt; indexOf(".") = -1 -&gt; decline.
    // ---------------------------------------------------------------------------------------------
    var fqdn = ('' + sourceValue).trim().toLowerCase();
    // -&gt; fqdn = "lva40bneehcs01.ecomm.devicenp.rpg".
    if (fqdn.indexOf('.') == -1)
        return null;
    // -&gt; indexOf(".") = 14 for the sample (the position of the first dot) -&gt; not -1 -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Search the whole CI table for a CI named with the full FQDN
    // What happens : opens a search on cmdb_ci, the root of every CI class, keeps only CIs whose
    //                name equals the complete lower-cased DNS name, and leaves out every class on
    //                the ignore list.
    // Why          : this is the only USEM rule that searches outside the hardware tree, which is
    //                why it runs last: every more precise rule has had its chance, and the ignore
    //                list still keeps placeholder classes out.
    // Sample       : the search on cmdb_ci for name = "lva40bneehcs01.ecomm.devicenp.rpg" finds the
    //                Virtual Machine Instance "lva40bneehcs01.ecomm.devicenp.rpg" (sys_id
    //                3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c).
    // ---------------------------------------------------------------------------------------------
    var gr = new GlideRecord('cmdb_ci');
    // -&gt; gr = a search on the root CI table, which covers every class.
    gr.addQuery('name', fqdn);
    // -&gt; condition added: name = "lva40bneehcs01.ecomm.devicenp.rpg" (the full string,
    //    case-insensitive).
    if (ignore)
        gr.addQuery('sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
    //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Decide: exactly one CI, or decline
    // What happens : runs the search, reads the first CI and accepts it only when there is no
    //                second CI in the result.
    // Why          : every finding of this host will be linked to the CI the rule returns. A wrong
    //                CI sends the findings to the wrong owner, so when two CIs share the value the
    //                rule refuses to guess and answers null; a later rule with different evidence
    //                may still resolve the host, and if none does the host stays unmatched for the
    //                CMDB team to review.
    // Sample       : one CI -&gt; the Virtual Machine Instance "lva40bneehcs01.ecomm.devicenp.rpg" -&gt;
    //                return "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". No CI -&gt; return null; the
    //                out-of-box Qualys rules (860 and above) get their turn and, if they also
    //                decline, the host stays unmatched for review. Two CIs -&gt; two CIs with that
    //                full name -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    gr.query();
    // -&gt; the search has run; gr now sits just before the first row of the result (0, 1 or more
    //    CIs).
    if (!gr.next())
        return null;
    // -&gt; next() moves to the first CI and returns true. When the result is empty it returns false
    //    and the rule declines with null.
    var match = gr.getUniqueValue();
    // -&gt; match = the 32-character sys_id of that CI, for example
    //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"; this is the value the framework expects back.
    if (gr.hasNext())
        return null;
    // -&gt; hasNext() is true when a second CI is waiting in the result: two owners of one value is an
    //    ambiguity, so the rule declines.
    return match;
    // -&gt; exactly one CI: its sys_id goes back to the framework, which links the vulnerable item to
    //    it and stops evaluating later rules.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;DNS&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:42:03&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;abff4d7e936a4310e3aef0aefaba1076&lt;/sys_id&gt;&lt;sys_mod_count&gt;2&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:20&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-1015484969</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>151fd5f2938b8710e3aef0aefaba10c3</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf4090000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM FQDN Name Broad Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>701fd5f25c8b87104ee8a3b3023ae083</update_guid>
<update_guid_history>701fd5f25c8b87104ee8a3b3023ae083:-1015484969,9a8a997efc0b871006e3e7a87b585976:-1175631375</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_ef4a4df2932a4310e3aef0aefaba1066</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Resolves the scanned DNS name through the layered CMDB model (DNS name -&amp;gt; IP address -&amp;gt; adapter -&amp;gt; owning CI) so aliases land on the device that answers on that name.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM Layered DNS Match&lt;/name&gt;&lt;order&gt;350&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;3&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 350 - USEM Layered DNS Match
   =================================================================================================
   PURPOSE
   Match through the layered network model that CMDB discovery maintains: a DNS Name record is
   linked to an IP Address record, the IP Address record belongs to a Network Adapter, and the
   adapter belongs to the CI. This finds hosts whose name is not written on the CI record at all,
   and it also resolves aliases.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "42773078",
     "IP": "167.202.60.26",
     "TRACKING_METHOD": "IP",
     "OS": "Red Hat Enterprise Linux Server 7.9",
     "DNS": "hklvteqoradbp3.hk.baml.com",
     "QG_HOSTID": "6337dede-02e7-0002-ad2c-0050569765df"
   }

   sourceValue   = the DNS field of that record -&gt; "hklvteqoradbp3.hk.baml.com"
   sourcePayload = the whole record above; the rule also reads the IP from it
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the CI at the end of the chain DNS Name
   "hklvteqoradbp3.hk.baml.com" -&gt; IP Address "167.202.60.26" -&gt; adapter "eth0" -&gt; Linux Server
   "hklvteqoradbp3"; when several CIs answer to the name, the one whose chain runs through the
   scanned IP is taken.

   WHY THIS RULE SITS AT ORDER 350
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 250 to 310 looked at name fields stored on the CI record itself.
   - Reaches it: hosts whose name lives only in the layered DNS records, and aliases that point at
                 an address of the device.
   - After it  : 400/410 (short hostname alone) and 450 (CI named with the full FQDN).

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a DNS name
    2. Clean the DNS name and require a domain part
    3. Note the scanned IP address for tie-breaks
    4. Read the list of CI classes that must never be matched
    5. Search the chain DNS Name -&gt; IP Address -&gt; adapter -&gt; CI
    6. Collect the owning CIs
    7. Decide: one owner, or the owner confirmed by the scanned IP
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a DNS name
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "hklvteqoradbp3.hk.baml.com" -&gt; not empty -&gt; the rule carries
    //                on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the DNS name and require a domain part
    // What happens : '' + sourceValue turns the value into plain text, trim() removes blanks at
    //                both ends and toLowerCase() lowers it. Then the rule requires at least one dot
    //                in the name.
    // Why          : DNS names are case-insensitive and CMDB values are stored in mixed case, so
    //                both sides are compared in lower case. A name without a dot is a bare
    //                hostname, which the hostname rules (400 and above) look after.
    // Sample       : fqdn = "hklvteqoradbp3.hk.baml.com"; indexOf(".") finds a dot -&gt; carry on. A
    //                bare label "hklvteqoradbp3" has no dot -&gt; indexOf(".") = -1 -&gt; decline.
    // ---------------------------------------------------------------------------------------------
    var fqdn = ('' + sourceValue).trim().toLowerCase();
    // -&gt; fqdn = "hklvteqoradbp3.hk.baml.com".
    if (fqdn.indexOf('.') == -1)
        return null;
    // -&gt; indexOf(".") = 14 for the sample (the position of the first dot) -&gt; not -1 -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Note the scanned IP address for tie-breaks
    // What happens : copies the IP field of the payload into plain text, or an empty text when the
    //                payload has none.
    // Why          : the IP is not used to search; it is only used later to choose between two CIs
    //                that both carry the scanned name.
    // Sample       : ip = "167.202.60.26".
    // ---------------------------------------------------------------------------------------------
    var ip = sourcePayload.IP ? '' + sourcePayload.IP : '';
    // -&gt; ip = "167.202.60.26" (or "" when the payload has no IP field).
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Search the chain DNS Name -&gt; IP Address -&gt; adapter -&gt; CI
    // What happens : searches the link table cmdb_ip_address_dns_name, whose rows tie one DNS Name
    //                record to one IP Address record. Through dot-walking (field.field.field) the
    //                conditions reach the other records of the chain: dns_name.name is the name on
    //                the DNS Name record, ip_address.nic.cmdb_ci is the CI that owns the adapter
    //                that holds the IP Address record. Rows whose chain ends nowhere, or on an
    //                ignored class, are left out.
    // Why          : discovery writes names and addresses as separate records linked to the device
    //                instead of on the device record; this is the only rule that can see those, and
    //                because the chain ends on whatever device actually holds the address, an alias
    //                resolves to the real machine.
    // Sample       : the link row DNS Name "hklvteqoradbp3.hk.baml.com" &lt;-&gt; IP Address
    //                "167.202.60.26"; the IP Address record belongs to adapter "eth0"; adapter
    //                "eth0" belongs to the Linux Server "hklvteqoradbp3" (sys_id
    //                3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c).
    // ---------------------------------------------------------------------------------------------
    var gr = new GlideRecord('cmdb_ip_address_dns_name');
    // -&gt; gr = a search on the IP Address to DNS Name link table.
    if (!gr.isValid())
        return null;
    // -&gt; the layered model is not installed on this instance -&gt; decline rather than fail.
    gr.addQuery('dns_name.name', fqdn);
    // -&gt; condition added: the linked DNS Name record is named "hklvteqoradbp3.hk.baml.com".
    gr.addNotNullQuery('ip_address.nic.cmdb_ci');
    // -&gt; condition added: the linked IP Address record belongs to an adapter (nic) that belongs to
    //    a CI (cmdb_ci); chains that stop early are left out.
    if (ignore)
        gr.addQuery('ip_address.nic.cmdb_ci.sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: the class of the CI at the end of the chain is NOT IN
    //    (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci, cmdb_ci_unclassed_hardware,
    //    cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    gr.query();
    // -&gt; the search has run; each row of the result is one name-to-address link whose chain ends on
    //    a CI.
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Collect the owning CIs
    // What happens : walks the result rows and records each distinct CI at the end of a chain;
    //                separately, records which of those CIs are reached through the scanned IP
    //                address.
    // Why          : one device with several addresses appears once per address, so each CI must be
    //                counted once; the IP bookkeeping prepares the tie-break of the next stage.
    // Sample       : one row -&gt; owners = {"3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c": true}, count = 1,
    //                first = "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"; because the row's IP Address
    //                record is "167.202.60.26", ipOwners = {"3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c":
    //                true}.
    // ---------------------------------------------------------------------------------------------
    var owners = {};
    var ipOwners = {};
    var count = 0, first = null;
    // -&gt; owners = {} (every CI seen, keyed by sys_id), ipOwners = {} (the CIs reached through the
    //    scanned IP), count = 0, first = null.
    while (gr.next()) {
        // -&gt; each pass of the loop looks at one link row.
        var owner = '' + gr.ip_address.nic.cmdb_ci;
        // -&gt; owner = the sys_id of the CI at the end of this row's chain, for example
        //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
        if (!owners[owner]) {
            owners[owner] = true;
            count++;
            if (count == 1)
                first = owner;
        }
        // -&gt; the first time a CI is seen: owners gains it, count grows by one, and first remembers
        //    the very first CI. A second row for the same CI changes nothing.
        if (ip &amp;&amp; ('' + gr.ip_address.ip_address) == ip)
            ipOwners[owner] = true;
        // -&gt; when this row's IP Address record equals the scanned IP "167.202.60.26", the CI is
        //    noted in ipOwners.
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 7 - Decide: one owner, or the owner confirmed by the scanned IP
    // What happens : accepts the single CI when exactly one was collected. When several CIs answer
    //                to the name, accepts the one CI that was reached through the scanned IP,
    //                provided there is exactly one such CI. Otherwise declines.
    // Why          : a name that resolves to two devices (an alias moved between hosts, an old and
    //                a new record) must not be guessed; the scanned address is the additional
    //                evidence that makes the choice safe.
    // Sample       : count = 1 -&gt; return "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". count = 2 with
    //                ipOwners = {"3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c": true} -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". count = 2 with ipOwners empty or holding
    //                both -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    if (count == 1)
        return first;
    // -&gt; one CI at the end of every chain -&gt; its sys_id goes back to the framework.
    if (count &gt; 1) {
        var confirmed = Object.keys(ipOwners);
        // -&gt; confirmed = the list of CIs reached through the scanned IP, for example
        //    ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"].
        if (confirmed.length == 1)
            return confirmed[0];
        // -&gt; exactly one CI confirmed by the address -&gt; return it.
    }
    return null;
    // -&gt; no CI, or several without a single IP confirmation -&gt; decline; the host continues to rule
    //    400.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;DNS&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:17:12&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;ef4a4df2932a4310e3aef0aefaba1066&lt;/sys_id&gt;&lt;sys_mod_count&gt;2&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:19&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>1224457235</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>191fd5f2938b8710e3aef0aefaba10c4</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf1310000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM Layered DNS Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>601fd5f2828b8710da7d2de2939fe83e</update_guid>
<update_guid_history>601fd5f2828b8710da7d2de2939fe83e:1224457235,d28a997e2f0b8710d565066270f11947:1099334146</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_321b2db4932ac754e3aef0aefaba1052</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Cisco Unified CM phones report SEP&amp;lt;MAC&amp;gt; as their DNS label. Derives the MAC and matches strictly within cmdb_ci_ip_phone (adapter, mac_address, or SEP device name).&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM Cisco IP Phone MAC&lt;/name&gt;&lt;order&gt;200&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;4&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 200 - USEM Cisco IP Phone MAC
   =================================================================================================
   PURPOSE
   Match Cisco IP phones. Cisco Unified Communications Manager names every phone "SEP" followed by
   the phone's MAC address, and Qualys reports that name as the DNS host label. The rule turns the
   label back into a MAC address and looks for exactly one IP Phone CI that carries it.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "41277345",
     "IP": "30.144.62.108",
     "TRACKING_METHOD": "IP",
     "OS": "Cisco IP Phone",
     "DNS": "sep64f69dd5c9b0.voip.bankofamerica.com"
   }

   sourceValue   = the DNS field of that record -&gt; "sep64f69dd5c9b0.voip.bankofamerica.com"
   sourcePayload = the whole record above
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the IP Phone CI whose network adapter, mac_address field or name
   carries the MAC 64:F6:9D:D5:C9:B0, for example the phone named "SEP64F69DD5C9B0"; its sys_id is
   returned. Any DNS name that does not follow the SEP pattern makes the rule decline at once.

   WHY THIS RULE SITS AT ORDER 200
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 175 and 180 handled serial numbers (phones report none).
   - Reaches it: only hosts whose DNS label reads SEP plus twelve hexadecimal characters; every
                 other host passes through untouched.
   - After it  : 250 and above are the name rules. Phones are resolved here first so a phone label
                 can never be mistaken for a server name, and so a MAC address clash can never pull
                 in a server or a switch: this rule only ever searches IP Phone CIs.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a DNS name
    2. Take the first label of the DNS name
    3. Recognise the SEP pattern and extract the MAC address
    4. Spell the MAC address the four ways CMDBs store it
    5. Read the list of CI classes that must never be matched
    6. Attempt 1: a network adapter with that MAC that belongs to an IP phone
    7. Attempt 2: the MAC stored on the IP phone record itself
    8. Attempt 3: an IP phone named with its Unified CM device name
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a DNS name
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "sep64f69dd5c9b0.voip.bankofamerica.com" -&gt; not empty -&gt; the
    //                rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Take the first label of the DNS name
    // What happens : splits the DNS name at the dots, keeps the first part and lowers it.
    // Why          : the phone identity is in the first label only; the rest
    //                ("voip.bankofamerica.com") is the telephony domain shared by every phone.
    // Sample       : "sep64f69dd5c9b0.voip.bankofamerica.com" -&gt; split(".") = ["sep64f69dd5c9b0",
    //                "voip", "bankofamerica", "com"] -&gt; [0] = "sep64f69dd5c9b0".
    // ---------------------------------------------------------------------------------------------
    var label = ('' + sourceValue).split('.')[0].toLowerCase();
    // -&gt; label = "sep64f69dd5c9b0".
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Recognise the SEP pattern and extract the MAC address
    // What happens : checks the label against the pattern "sep" followed by exactly twelve
    //                hexadecimal characters (0 to 9, a to f) and nothing else. When it fits, the
    //                twelve characters are kept as the MAC address.
    // Why          : a server named "sepulveda01" must not be treated as a phone, so the pattern is
    //                strict: exactly "sep", exactly twelve hex characters, from the start to the
    //                end of the label.
    // Sample       : "sep64f69dd5c9b0" fits -&gt; hex = "64f69dd5c9b0". "wsaoi01zeapd1" does not fit
    //                -&gt; the rule declines and the host goes on to the name rules.
    // ---------------------------------------------------------------------------------------------
    var m = label.match(/^sep([0-9a-f]{12})$/);
    // -&gt; m = ["sep64f69dd5c9b0", "64f69dd5c9b0"] (the whole match and the captured twelve
    //    characters), or null when the label does not fit.
    if (!m)
        return null;
    // -&gt; m is null for every host that is not a phone -&gt; decline; for the sample m is filled -&gt;
    //    carry on.
    var hex = m[1];
    // -&gt; hex = "64f69dd5c9b0".
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Spell the MAC address the four ways CMDBs store it
    // What happens : cuts the twelve characters into six pairs, joins them with colons, and
    //                prepares four spellings: colon-separated upper case, colon-separated lower
    //                case, plain upper case and plain lower case.
    // Why          : different discovery tools write MAC addresses differently; searching all four
    //                spellings at once means the phone is found whichever tool created its record.
    // Sample       : pairs = ["64", "f6", "9d", "d5", "c9", "b0"]; colon = "64:f6:9d:d5:c9:b0";
    //                candidates = ["64:F6:9D:D5:C9:B0", "64:f6:9d:d5:c9:b0", "64F69DD5C9B0",
    //                "64f69dd5c9b0"].
    // ---------------------------------------------------------------------------------------------
    var pairs = [];
    // -&gt; pairs = [] (empty list to start with).
    for (var i = 0; i &lt; 12; i += 2)
        pairs.push(hex.substr(i, 2));
    // -&gt; i takes the values 0, 2, 4, 6, 8, 10; substr(i, 2) cuts two characters at each position -&gt;
    //    pairs = ["64", "f6", "9d", "d5", "c9", "b0"].
    var colon = pairs.join(':');
    // -&gt; colon = "64:f6:9d:d5:c9:b0".
    var candidates = [colon.toUpperCase(), colon, hex.toUpperCase(), hex];
    // -&gt; candidates = ["64:F6:9D:D5:C9:B0", "64:f6:9d:d5:c9:b0", "64F69DD5C9B0", "64f69dd5c9b0"].
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Attempt 1: a network adapter with that MAC that belongs to an IP phone
    // What happens : searches the Network Adapter table for adapters whose mac_address is one of
    //                the four spellings and that belong to a CI, then keeps only the owners that
    //                really are IP Phone CIs, each counted once.
    // Why          : discovery tools usually store the MAC on the adapter record, not on the phone
    //                itself. Checking that the owner is an IP Phone guarantees that a switch or
    //                server adapter with a colliding MAC is never returned.
    // Sample       : adapter "eth0" with mac_address "64:F6:9D:D5:C9:B0" belongs to the IP Phone
    //                "SEP64F69DD5C9B0" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c) -&gt; phones =
    //                {"3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c": true}, count = 1, first =
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c" -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    // ---------------------------------------------------------------------------------------------
    var nic = new GlideRecord('cmdb_ci_network_adapter');
    // -&gt; nic = a search on the Network Adapter table.
    nic.addQuery('mac_address', 'IN', candidates.join(','));
    // -&gt; condition added: mac_address IN ("64:F6:9D:D5:C9:B0", "64:f6:9d:d5:c9:b0", "64F69DD5C9B0",
    //    "64f69dd5c9b0").
    nic.addNotNullQuery('cmdb_ci');
    // -&gt; condition added: cmdb_ci is not empty, so only adapters that belong to a CI are
    //    considered.
    nic.query();
    // -&gt; the search has run; nic sits before the first adapter of the result.
    var phones = {};
    var count = 0, first = null;
    // -&gt; phones = {} (phone CIs already counted, keyed by sys_id), count = 0, first = null.
    while (nic.next()) {
        // -&gt; each pass of the loop looks at one adapter carrying the MAC.
        var owner = nic.getValue('cmdb_ci');
        // -&gt; owner = the sys_id of the CI that owns the adapter, for example
        //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
        var phone = new GlideRecord('cmdb_ci_ip_phone');
        if (phone.get(owner) &amp;&amp; !phones[owner]) {
            // -&gt; phone.get(owner) is true only when that CI is an IP Phone; !phones[owner] is true
            //    the first time this phone is seen.
            phones[owner] = true;
            count++;
            if (count == 1)
                first = owner;
            // -&gt; phones = {"3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c": true}, count = 1, first =
            //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c" after the first phone; a second, different
            //    phone would make count = 2.
        }
        // -&gt; an adapter owned by a server or a switch is skipped here, so it can never be counted.
    }
    if (count == 1)
        return first;
    // -&gt; exactly one phone owns an adapter with this MAC -&gt; return its sys_id
    //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". Zero phones -&gt; attempt 2. Two phones -&gt; attempt 2 as
    //    well, the record itself may settle it.
    // ---------------------------------------------------------------------------------------------
    // STAGE 7 - Attempt 2: the MAC stored on the IP phone record itself
    // What happens : searches the IP Phone table for phones whose own mac_address field is one of
    //                the four spellings (ignored classes left out) and accepts the phone only when
    //                it is the single one.
    // Why          : some loads write the MAC on the phone record instead of an adapter record;
    //                this attempt covers them without ever leaving the IP Phone class.
    // Sample       : the IP Phone "SEP64F69DD5C9B0" with mac_address = "64F69DD5C9B0" -&gt; byMac =
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c" and no second row -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    // ---------------------------------------------------------------------------------------------
    var ph = new GlideRecord('cmdb_ci_ip_phone');
    // -&gt; ph = a search on the IP Phone class.
    ph.addQuery('mac_address', 'IN', candidates.join(','));
    // -&gt; condition added: mac_address IN (the four spellings).
    if (ignore)
        ph.addQuery('sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
    //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    ph.query();
    // -&gt; the search has run.
    if (ph.next()) {
        // -&gt; at least one phone carries the MAC on its record.
        var byMac = ph.getUniqueValue();
        // -&gt; byMac = the sys_id of that phone, for example "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
        if (!ph.hasNext())
            return byMac;
        // -&gt; no second phone -&gt; return "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". A second phone with the
        //    same MAC -&gt; fall through to attempt 3.
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 8 - Attempt 3: an IP phone named with its Unified CM device name
    // What happens : searches the IP Phone table for a phone whose name is the label in upper case
    //                ("SEP64F69DD5C9B0"), the device name Unified CM assigns, and accepts it only
    //                when it is the single one.
    // Why          : a phone loaded from the call manager export has no MAC on either record, but
    //                it is named exactly like the DNS label; this attempt catches those without
    //                loosening any check.
    // Sample       : the IP Phone named "SEP64F69DD5C9B0" -&gt; named =
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c" and no second row -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". No phone at all -&gt; return null and the
    //                host continues to rule 250.
    // ---------------------------------------------------------------------------------------------
    var byName = new GlideRecord('cmdb_ci_ip_phone');
    // -&gt; byName = a search on the IP Phone class.
    byName.addQuery('name', label.toUpperCase());
    // -&gt; condition added: name = "SEP64F69DD5C9B0".
    if (ignore)
        byName.addQuery('sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
    //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    byName.query();
    // -&gt; the search has run.
    if (!byName.next())
        return null;
    // -&gt; no phone with that name -&gt; decline; the host goes on to the name rules, which will not
    //    recognise a SEP label either, so it ends up unmatched for review.
    var named = byName.getUniqueValue();
    // -&gt; named = "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    if (byName.hasNext())
        return null;
    // -&gt; two phones with the same device name -&gt; ambiguous -&gt; decline.
    return named;
    // -&gt; exactly one phone -&gt; its sys_id goes back to the framework.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;DNS&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-04 11:32:40&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;321b2db4932ac754e3aef0aefaba1052&lt;/sys_id&gt;&lt;sys_mod_count&gt;3&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:19&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-319702582</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>1d1fd5f2938b8710e3aef0aefaba10c0</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5befe10000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM Cisco IP Phone MAC</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>601fd5f2eb8b8710d7c1f2832e3ab32a</update_guid>
<update_guid_history>601fd5f2eb8b8710d7c1f2832e3ab32a:-319702582,9a8a597ed60b8710f8d45c5e8256a7fa:1702364773,54720274d36ec75405eaaf122cc82a16:-1848102433</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_3e1ba9f493e6c754e3aef0aefaba10cb</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Serial number resolved strictly inside the CI class implied by the scanned OS. Junk serials filtered; unique owner required.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM Serial Number Class Match&lt;/name&gt;&lt;order&gt;175&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;5&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 175 - USEM Serial Number Class Match
   =================================================================================================
   PURPOSE
   Match a scanned host to its CI by serial number, but only inside the CMDB class that the scanned
   operating system implies. A serial number is the strongest identifier a physical or virtual
   machine has, so this is the first USEM rule in the chain.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "35832680",
     "IP": "171.128.225.96",
     "TRACKING_METHOD": "AGENT",
     "OS": "Red Hat Enterprise Linux 9.8",
     "DNS": "ah-1047132-001.sdi.corp.bankofamerica.com",
     "SERIAL_NUMBER": "VMware-42 1a 9c 3f 7d 2e 61 b8-55 04 e2 91 6a 27 c3 08"
   }

   sourceValue   = the SERIAL_NUMBER field of that record -&gt; "VMware-42 1a 9c 3f 7d 2e 61 b8-55 04 e2 91 6a 27 c3 08"
   sourcePayload = the whole record above; the rule also reads the OS from it
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the Linux Server CI "ah-1047132-001" whose serial_number field
   holds the same serial; its sys_id is returned. If no Linux Server carries the serial, or two do,
   the rule returns null.

   WHY THIS RULE SITS AT ORDER 175
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : nothing custom runs before it. Serial numbers come first because a serial belongs
                 to one machine for its whole life, while names and addresses are reused.
   - Reaches it: every Qualys host that reports a SERIAL_NUMBER. Today only hosts scanned by the
                 authenticated Cloud Agent report one; unauthenticated network scans leave the field
                 empty and those hosts skip straight to the name rules.
   - After it  : 180 repeats the serial search across every hardware class for hosts whose OS gives
                 no class or whose CI is classed differently; 200 and above move on to names and,
                 last of all, addresses.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a serial number
    2. Clean the serial number
    3. Reject placeholder serial numbers
    4. Read the list of CI classes that must never be matched
    5. Work out the CMDB class from the scanned operating system
    6. Search that class for the serial
    7. Decide: exactly one CI, or decline
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a serial number
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "VMware-42 1a 9c 3f 7d 2e 61 b8-55 04 e2 91 6a 27 c3 08" -&gt; not
    //                empty -&gt; the rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the serial number
    // What happens : '' + sourceValue turns the platform value into a plain text string and trim()
    //                removes blanks at both ends. Blanks inside the value stay, because they are
    //                part of the serial.
    // Why          : some feeds pad values with blanks; a search for " MXQ13005TC " would not find
    //                "MXQ13005TC".
    // Sample       : serial = "VMware-42 1a 9c 3f 7d 2e 61 b8-55 04 e2 91 6a 27 c3 08".
    // ---------------------------------------------------------------------------------------------
    var serial = ('' + sourceValue).trim();
    // -&gt; serial = "VMware-42 1a 9c 3f 7d 2e 61 b8-55 04 e2 91 6a 27 c3 08" (54 characters).
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Reject placeholder serial numbers
    // What happens : the rule keeps a list of placeholder serials that vendors ship on thousands of
    //                machines ("To Be Filled By O.E.M.", "System Serial Number", "0123456789",
    //                "None" and so on) and refuses to search for any of them, or for a serial
    //                shorter than four characters.
    // Why          : a placeholder would match dozens of CIs at once and the tie could never be
    //                broken; declining at once lets the name and address rules do the work instead.
    // Sample       : "vmware-42 1a 9c 3f 7d 2e 61 b8-55 04 e2 91 6a 27 c3 08" is 54 characters long
    //                and is not in the list -&gt; carry on. "N/A" would be found as ",n/a," in the
    //                list -&gt; decline. "12" is shorter than four characters -&gt; decline.
    // ---------------------------------------------------------------------------------------------
    var junk = ',0,none,n/a,na,unknown,empty,not specified,not available,no serial,' +
        'default string,to be filled by o.e.m.,system serial number,chassis serial number,' +
        '0123456789,1234567890,';
    // -&gt; junk = one long text with a comma before and after every placeholder, so that "," + serial
    //    + "," can only be found when the whole serial equals a placeholder (",na," is never found
    //    inside ",not available,").
    if (serial.length &lt; 4 || junk.indexOf(',' + serial.toLowerCase() + ',') != -1)
        return null;
    // -&gt; serial.toLowerCase() = "vmware-42 1a 9c 3f 7d 2e 61 b8-55 04 e2 91 6a 27 c3 08";
    //    junk.indexOf(",vmware-42 1a 9c 3f 7d 2e 61 b8-55 04 e2 91 6a 27 c3 08,") = -1 (not found)
    //    and the length is 54 -&gt; the condition is false -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Work out the CMDB class from the scanned operating system
    // What happens : the helper classFor() reads the operating system text Qualys reports and
    //                returns the CMDB class such a machine is stored in. It lowers the text, gives
    //                up when the text is an unauthenticated multi-guess fingerprint (three or more
    //                guesses separated by "/"), then looks for key words in a fixed order and
    //                returns an empty string when nothing fits. Searching a class also searches
    //                every class beneath it, so cmdb_ci_linux_server covers all Linux flavour
    //                classes.
    // Why          : evidence is only trusted when it lands in a class that agrees with the scanned
    //                OS: a Red Hat host must resolve to a Linux Server CI, never to a Windows
    //                Server that happens to carry the same value.
    // Sample       : classFor("Red Hat Enterprise Linux 9.8"): s = "red hat enterprise linux 9.8";
    //                s.split("/") has 1 part (not a multi-guess); "esx", "windows", "aix",
    //                "solaris", "sunos", "hp-ux", "netapp", "ontap", "printer", "laserjet" and
    //                "jetdirect" are not found; "red hat" is found -&gt; returns
    //                "cmdb_ci_linux_server" (Linux Server).
    // ---------------------------------------------------------------------------------------------
    function classFor(os) {
        if (!os) return '';
        // -&gt; no OS text at all -&gt; "" (no class).
        var s = ('' + os).toLowerCase();
        // -&gt; s = the OS text in lower case, so that "Windows", "WINDOWS" and "windows" all compare
        //    the same.
        if (s.split('/').length &gt; 2) return '';
        // -&gt; "ubuntu / tiny core linux / linux 2.6.x / ibm asm / hp storeonce / f5 networks big-ip
        //    / cisco ios software" splits into 7 parts at the "/" characters -&gt; more than 2 -&gt; an
        //    unauthenticated guess list -&gt; "" (no class).
        if (s.indexOf('esx') != -1) return 'cmdb_ci_esx_server';
        // -&gt; "vmware esxi 7.0.3 build 24723872" contains "esx" -&gt; "cmdb_ci_esx_server" (ESX
        //    Server).
        if (s.indexOf('windows') != -1)
            return s.indexOf('server') != -1 ? 'cmdb_ci_win_server' : 'cmdb_ci_computer';
        // -&gt; "windows server 2016 standard 64 bit edition version 1607" -&gt; "cmdb_ci_win_server"
        //    (Windows Server); "windows 10 enterprise 64 bit edition version 22h2" has no "server"
        //    -&gt; "cmdb_ci_computer" (Computer, a workstation).
        if (s.indexOf('aix') != -1) return 'cmdb_ci_aix_server';
        // -&gt; "aix 7.3 tl3" -&gt; "cmdb_ci_aix_server" (AIX Server).
        if (s.indexOf('solaris') != -1 || s.indexOf('sunos') != -1) return 'cmdb_ci_solaris_server';
        // -&gt; "oracle solaris 11.4" -&gt; "cmdb_ci_solaris_server" (Solaris Server).
        if (s.indexOf('hp-ux') != -1) return 'cmdb_ci_hpux_server';
        // -&gt; "hp-ux b.11.31" -&gt; "cmdb_ci_hpux_server" (HPUX Server).
        if (s.indexOf('netapp') != -1 || s.indexOf('ontap') != -1) return 'cmdb_ci_storage_server';
        // -&gt; "netapp ontap 9.12.1" -&gt; "cmdb_ci_storage_server" (Storage Server).
        if (s.indexOf('printer') != -1 || s.indexOf('laserjet') != -1 || s.indexOf('jetdirect') != -1) return 'cmdb_ci_printer';
        // -&gt; "hp laserjet m507" -&gt; "cmdb_ci_printer" (Printer).
        if (s.indexOf('red hat') != -1 || s.indexOf('linux') != -1 || s.indexOf('centos') != -1 ||
            s.indexOf('ubuntu') != -1 || s.indexOf('suse') != -1 || s.indexOf('debian') != -1 ||
            s.indexOf('fedora') != -1 || s.indexOf('euleros') != -1 ||
            s.indexOf('oracle enterprise') != -1 || s.indexOf('amazon') != -1) return 'cmdb_ci_linux_server';
        // -&gt; "red hat enterprise linux 9.8", "suse linux enterprise server 15 sp5" and "amazon
        //    linux 2023" -&gt; "cmdb_ci_linux_server" (Linux Server).
        if (s.indexOf('nx-os') != -1 || s.indexOf('catos') != -1 || s.indexOf('cisco') != -1) return 'cmdb_ci_netgear';
        // -&gt; "cisco nx-os 9.3(8)" -&gt; "cmdb_ci_netgear" (Network Gear).
        return '';
        // -&gt; anything else, for example "unknown" -&gt; "" (no class).
    }

    var pref = classFor(sourcePayload.OS);
    // -&gt; pref = "cmdb_ci_linux_server" for the sample OS "Red Hat Enterprise Linux 9.8".
    if (!pref)
        return null;
    // -&gt; pref = "" happens for an unknown OS or a multi-guess fingerprint; this rule then declines
    //    so that rule 180 (which searches all hardware without a class) gets its turn.
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Search that class for the serial
    // What happens : opens a search on the class chosen above (which automatically includes its
    //                sub-classes), keeps only CIs whose serial_number equals the cleaned serial,
    //                and leaves out every class on the ignore list.
    // Why          : the serial must match exactly (no wildcard) and inside the agreed class only,
    //                so class evidence and serial evidence have to agree before the rule trusts the
    //                result.
    // Sample       : the search on cmdb_ci_linux_server for serial_number = "VMware-42 1a 9c 3f 7d
    //                2e 61 b8-55 04 e2 91 6a 27 c3 08" finds the Linux Server "ah-1047132-001"
    //                (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c). A Windows Server with the same
    //                serial is outside the class and never appears in the result.
    // ---------------------------------------------------------------------------------------------
    var gr = new GlideRecord(pref);
    // -&gt; gr = a search on the Linux Server class and every class beneath it; nothing has run yet.
    if (!gr.isValid())
        return null;
    // -&gt; isValid() is false only when the class does not exist on this instance (a plugin not
    //    installed); then the rule declines rather than fail.
    gr.addQuery('serial_number', serial);
    // -&gt; condition added: serial_number = "VMware-42 1a 9c 3f 7d 2e 61 b8-55 04 e2 91 6a 27 c3 08"
    //    (exact match, case-insensitive).
    if (ignore)
        gr.addQuery('sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
    //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name), so an ignored class
    //    can never appear in the result.
    // ---------------------------------------------------------------------------------------------
    // STAGE 7 - Decide: exactly one CI, or decline
    // What happens : runs the search, reads the first CI and accepts it only when there is no
    //                second CI in the result.
    // Why          : every finding of this host will be linked to the CI the rule returns. A wrong
    //                CI sends the findings to the wrong owner, so when two CIs share the value the
    //                rule refuses to guess and answers null; a later rule with different evidence
    //                may still resolve the host, and if none does the host stays unmatched for the
    //                CMDB team to review.
    // Sample       : one CI -&gt; the Linux Server "ah-1047132-001" -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". No CI -&gt; return null and rule 180 gets its
    //                turn. Two CIs -&gt; "ah-1047132-001" and a second Linux Server loaded twice with
    //                the same serial -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    gr.query();
    // -&gt; the search has run; gr now sits just before the first row of the result (0, 1 or more
    //    CIs).
    if (!gr.next())
        return null;
    // -&gt; next() moves to the first CI and returns true. When the result is empty it returns false
    //    and the rule declines with null.
    var match = gr.getUniqueValue();
    // -&gt; match = the 32-character sys_id of that CI, for example
    //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"; this is the value the framework expects back.
    if (gr.hasNext())
        return null;
    // -&gt; hasNext() is true when a second CI is waiting in the result: two owners of one value is an
    //    ambiguity, so the rule declines.
    return match;
    // -&gt; exactly one CI: its sys_id goes back to the framework, which links the vulnerable item to
    //    it and stops evaluating later rules.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;SERIAL_NUMBER&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-04 11:32:40&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;3e1ba9f493e6c754e3aef0aefaba10cb&lt;/sys_id&gt;&lt;sys_mod_count&gt;4&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:19&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-891047450</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>511fd5f2938b8710e3aef0aefaba10c1</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bef2f0000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM Serial Number Class Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>241f95f28a8b871049ed27a7bb9a8ef0</update_guid>
<update_guid_history>241f95f28a8b871049ed27a7bb9a8ef0:-891047450,5a8a597e530b871004715f28819244ce:-439581464,c4720274c26ec7545b2b55142f449006:-888082438</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_97ff4d7e936a4310e3aef0aefaba1010</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Exact FQDN across the cmdb_ci_hardware subtree; duplicate FQDNs resolve only when the scanned IP corroborates a single candidate.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM FQDN Hardware Match&lt;/name&gt;&lt;order&gt;260&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;3&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 260 - USEM FQDN Hardware Match
   =================================================================================================
   PURPOSE
   Match by exact FQDN anywhere in the hardware tree. This is the second FQDN stage, for hosts whose
   OS gives no class or whose CI is classed differently from the OS.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "35920204",
     "IP": "171.135.28.125",
     "TRACKING_METHOD": "IP",
     "OS": "Ubuntu / Tiny Core Linux / Linux 2.6.x / IBM ASM / HP StoreOnce / F5 Networks Big-IP / Cisco IOS Software",
     "DNS": "txr9gxcenah031.sdi.corp.bankofamerica.com"
   }

   sourceValue   = the DNS field of that record -&gt; "txr9gxcenah031.sdi.corp.bankofamerica.com"
   sourcePayload = the whole record above; the rule also reads the IP from it
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the one hardware CI whose fqdn is
   "txr9gxcenah031.sdi.corp.bankofamerica.com", for example the Server CI "txr9gxcenah031";
   duplicates are resolved by the IP 171.135.28.125 or declined.

   WHY THIS RULE SITS AT ORDER 260
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 250 tried the exact FQDN inside the class the OS implies.
   - Reaches it: hosts whose OS gives no class (the sample is an unauthenticated scan with seven
                 guesses) or whose CI is classed differently from the OS, so 250 found nothing.
   - After it  : 300 and 310 use hostname plus domain evidence for CIs that carry no fqdn value at
                 all.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a DNS name
    2. Clean the DNS name and require a domain part
    3. Note the scanned IP address for tie-breaks
    4. Read the list of CI classes that must never be matched
    5. The helper pickFqdn(table)
    6. Run the helper on the whole hardware tree
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a DNS name
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "txr9gxcenah031.sdi.corp.bankofamerica.com" -&gt; not empty -&gt; the
    //                rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the DNS name and require a domain part
    // What happens : '' + sourceValue turns the value into plain text, trim() removes blanks at
    //                both ends and toLowerCase() lowers it. Then the rule requires at least one dot
    //                in the name.
    // Why          : DNS names are case-insensitive and CMDB values are stored in mixed case, so
    //                both sides are compared in lower case. A name without a dot is a bare
    //                hostname, which the hostname rules (400 and above) look after.
    // Sample       : fqdn = "txr9gxcenah031.sdi.corp.bankofamerica.com"; indexOf(".") finds a dot
    //                -&gt; carry on. A bare label "txr9gxcenah031" has no dot -&gt; indexOf(".") = -1 -&gt;
    //                decline.
    // ---------------------------------------------------------------------------------------------
    var fqdn = ('' + sourceValue).trim().toLowerCase();
    // -&gt; fqdn = "txr9gxcenah031.sdi.corp.bankofamerica.com".
    if (fqdn.indexOf('.') == -1)
        return null;
    // -&gt; indexOf(".") = 14 for the sample (the position of the first dot) -&gt; not -1 -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Note the scanned IP address for tie-breaks
    // What happens : copies the IP field of the payload into plain text, or an empty text when the
    //                payload has none.
    // Why          : the IP is not used to search; it is only used later to choose between two CIs
    //                that both carry the scanned name.
    // Sample       : ip = "171.135.28.125".
    // ---------------------------------------------------------------------------------------------
    var ip = sourcePayload.IP ? '' + sourcePayload.IP : '';
    // -&gt; ip = "171.135.28.125" (or "" when the payload has no IP field).
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - The helper pickFqdn(table)
    // What happens : defines the helper used in the next stage. It searches one table for CIs whose
    //                fqdn field equals the scanned name (ignored classes left out), collects the
    //                sys_id of every CI found, notes which of them also carry the scanned IP
    //                address, and decides: one CI -&gt; that CI; several CIs but exactly one with the
    //                scanned IP -&gt; that one; anything else -&gt; null.
    // Why          : an FQDN is meant to be unique, but CMDBs do carry duplicates: a retired server
    //                and its rebuilt replacement, or a cluster alias loaded on two nodes. The
    //                scanned IP is the second piece of evidence that can safely break such a tie;
    //                without it the rule declines rather than guess.
    // Sample       : the Server "txr9gxcenah031" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c) has fqdn
    //                = "txr9gxcenah031.sdi.corp.bankofamerica.com" -&gt; ids =
    //                ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"], ipHits =
    //                ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"] because its ip_address is also
    //                "171.135.28.125" -&gt; ids.length == 1 -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    // ---------------------------------------------------------------------------------------------
    function pickFqdn(table) {
        var gr = new GlideRecord(table);
        // -&gt; gr = a search on the table given (the class and every class beneath it); nothing has
        //    run yet
        if (!gr.isValid())
            return null;
        // -&gt; isValid() is false only when the class does not exist on this instance; then the
        //    helper declines rather than fail
        gr.addQuery('fqdn', fqdn);
        // -&gt; condition added: fqdn = "txr9gxcenah031.sdi.corp.bankofamerica.com" (exact match,
        //    case-insensitive)
        if (ignore)
            gr.addQuery('sys_class_name', 'NOT IN', ignore);
        // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
        //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name)
        gr.query();
        // -&gt; the search has run; gr sits before the first row of the result
        var ids = [];
        var ipHits = [];
        // -&gt; two empty lists: ids will hold every CI found, ipHits the ones that also carry the
        //    scanned IP
        while (gr.next()) {
            // -&gt; each pass of the loop looks at one CI of the result
            ids.push(gr.getUniqueValue());
            // -&gt; ids = ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"] after the first CI,
            //    ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c", "8c1d5e2f7a9b4c3d6e0f1a2b3c4d5e6f"] when a
            //    second one exists
            if (ip &amp;&amp; gr.getValue('ip_address') == ip)
                ipHits.push(gr.getUniqueValue());
            // -&gt; ipHits = ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"] when this CI's ip_address field is
            //    "171.135.28.125"; unchanged otherwise
        }
        if (ids.length == 1)
            return ids[0];
        // -&gt; exactly one CI carries the FQDN -&gt; its sys_id is the answer
        if (ids.length &gt; 1 &amp;&amp; ipHits.length == 1)
            return ipHits[0];
        // -&gt; two or more CIs carry the FQDN and exactly one of them also has the scanned IP -&gt; that
        //    one is the answer
        return null;
        // -&gt; no CI, or several CIs without a single IP confirmation -&gt; decline
    }

    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Run the helper on the whole hardware tree
    // ---------------------------------------------------------------------------------------------
    return pickFqdn('cmdb_ci_hardware');
    // -&gt; pickFqdn("cmdb_ci_hardware") searches Hardware and every class beneath it -&gt;
    //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c" for the sample; null makes the host continue to rule
    //    300.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;DNS&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:42:02&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;97ff4d7e936a4310e3aef0aefaba1010&lt;/sys_id&gt;&lt;sys_mod_count&gt;2&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:19&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-2092961695</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>551fd5f2938b8710e3aef0aefaba10c2</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf0780000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM FQDN Hardware Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>601fd5f2ce8b8710b6c88d730be16732</update_guid>
<update_guid_history>601fd5f2ce8b8710b6c88d730be16732:-2092961695,1a8a997eab0b87105f4464ddcbe81400:1233037585</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_b3f9e0a493a60754e3aef0aefaba102f</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Exact FQDN inside the OS-implied class; duplicate FQDNs resolve only when the scanned IP corroborates a single candidate.&lt;/description&gt;&lt;lookup_script display_value="DNS"&gt;89269effc3120300a1cedb1122d3ae79&lt;/lookup_script&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM FQDN Class Match&lt;/name&gt;&lt;order&gt;250&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;6&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 250 - USEM FQDN Class Match
   =================================================================================================
   PURPOSE
   Match a scanned host by its fully qualified domain name (FQDN) stored in the fqdn field of a CI,
   inside the CMDB class the scanned OS implies. An exact FQDN on the CI is the most precise name
   evidence there is, so this is the first name rule after the phone rule.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "83047612",
     "IP": "30.206.199.36",
     "TRACKING_METHOD": "IP",
     "OS": "VMware ESXi 7.0.3 build 24723872",
     "DNS": "vsdnac22xsdi004.sdi.corp.bankofamerica.com"
   }

   sourceValue   = the DNS field of that record -&gt; "vsdnac22xsdi004.sdi.corp.bankofamerica.com"
   sourcePayload = the whole record above; the rule also reads the OS and the IP from it
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the ESX Server CI whose fqdn field is
   "vsdnac22xsdi004.sdi.corp.bankofamerica.com"; its sys_id is returned. Two ESX Servers with that
   fqdn are accepted only when exactly one of them also carries the IP 30.206.199.36.

   WHY THIS RULE SITS AT ORDER 250
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 175/180 (serial numbers) and 200 (phone labels) found nothing.
   - Reaches it: every host with a dotted DNS name, which is the bulk of the Qualys feed.
   - After it  : 260 repeats the exact FQDN search across every hardware class; 300 and 310 fall
                 back to hostname plus domain evidence; 400 and 410 to the short hostname alone.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a DNS name
    2. Clean the DNS name and require a domain part
    3. Note the scanned IP address for tie-breaks
    4. Read the list of CI classes that must never be matched
    5. Work out the CMDB class from the scanned operating system
    6. The helper pickFqdn(table)
    7. Run the helper inside the class the OS implies
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a DNS name
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "vsdnac22xsdi004.sdi.corp.bankofamerica.com" -&gt; not empty -&gt; the
    //                rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the DNS name and require a domain part
    // What happens : '' + sourceValue turns the value into plain text, trim() removes blanks at
    //                both ends and toLowerCase() lowers it. Then the rule requires at least one dot
    //                in the name.
    // Why          : DNS names are case-insensitive and CMDB values are stored in mixed case, so
    //                both sides are compared in lower case. A name without a dot is a bare
    //                hostname, which the hostname rules (400 and above) look after.
    // Sample       : fqdn = "vsdnac22xsdi004.sdi.corp.bankofamerica.com"; indexOf(".") finds a dot
    //                -&gt; carry on. A bare label "vsdnac22xsdi004" has no dot -&gt; indexOf(".") = -1 -&gt;
    //                decline.
    // ---------------------------------------------------------------------------------------------
    var fqdn = ('' + sourceValue).trim().toLowerCase();
    // -&gt; fqdn = "vsdnac22xsdi004.sdi.corp.bankofamerica.com".
    if (fqdn.indexOf('.') == -1)
        return null;
    // -&gt; indexOf(".") = 15 for the sample (the position of the first dot) -&gt; not -1 -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Note the scanned IP address for tie-breaks
    // What happens : copies the IP field of the payload into plain text, or an empty text when the
    //                payload has none.
    // Why          : the IP is not used to search; it is only used later to choose between two CIs
    //                that both carry the scanned name.
    // Sample       : ip = "30.206.199.36".
    // ---------------------------------------------------------------------------------------------
    var ip = sourcePayload.IP ? '' + sourcePayload.IP : '';
    // -&gt; ip = "30.206.199.36" (or "" when the payload has no IP field).
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Work out the CMDB class from the scanned operating system
    // What happens : the helper classFor() reads the operating system text Qualys reports and
    //                returns the CMDB class such a machine is stored in. It lowers the text, gives
    //                up when the text is an unauthenticated multi-guess fingerprint (three or more
    //                guesses separated by "/"), then looks for key words in a fixed order and
    //                returns an empty string when nothing fits. Searching a class also searches
    //                every class beneath it, so cmdb_ci_linux_server covers all Linux flavour
    //                classes.
    // Why          : evidence is only trusted when it lands in a class that agrees with the scanned
    //                OS: a Red Hat host must resolve to a Linux Server CI, never to a Windows
    //                Server that happens to carry the same value.
    // Sample       : classFor("VMware ESXi 7.0.3 build 24723872"): s = "vmware esxi 7.0.3 build
    //                24723872"; s.split("/") has 1 part; "esx" is found straight away -&gt; returns
    //                "cmdb_ci_esx_server" (ESX Server).
    // ---------------------------------------------------------------------------------------------
    function classFor(os) {
        if (!os) return '';
        // -&gt; no OS text at all -&gt; "" (no class).
        var s = ('' + os).toLowerCase();
        // -&gt; s = the OS text in lower case, so that "Windows", "WINDOWS" and "windows" all compare
        //    the same.
        if (s.split('/').length &gt; 2) return '';
        // -&gt; "ubuntu / tiny core linux / linux 2.6.x / ibm asm / hp storeonce / f5 networks big-ip
        //    / cisco ios software" splits into 7 parts at the "/" characters -&gt; more than 2 -&gt; an
        //    unauthenticated guess list -&gt; "" (no class).
        if (s.indexOf('esx') != -1) return 'cmdb_ci_esx_server';
        // -&gt; "vmware esxi 7.0.3 build 24723872" contains "esx" -&gt; "cmdb_ci_esx_server" (ESX
        //    Server).
        if (s.indexOf('windows') != -1)
            return s.indexOf('server') != -1 ? 'cmdb_ci_win_server' : 'cmdb_ci_computer';
        // -&gt; "windows server 2016 standard 64 bit edition version 1607" -&gt; "cmdb_ci_win_server"
        //    (Windows Server); "windows 10 enterprise 64 bit edition version 22h2" has no "server"
        //    -&gt; "cmdb_ci_computer" (Computer, a workstation).
        if (s.indexOf('aix') != -1) return 'cmdb_ci_aix_server';
        // -&gt; "aix 7.3 tl3" -&gt; "cmdb_ci_aix_server" (AIX Server).
        if (s.indexOf('solaris') != -1 || s.indexOf('sunos') != -1) return 'cmdb_ci_solaris_server';
        // -&gt; "oracle solaris 11.4" -&gt; "cmdb_ci_solaris_server" (Solaris Server).
        if (s.indexOf('hp-ux') != -1) return 'cmdb_ci_hpux_server';
        // -&gt; "hp-ux b.11.31" -&gt; "cmdb_ci_hpux_server" (HPUX Server).
        if (s.indexOf('netapp') != -1 || s.indexOf('ontap') != -1) return 'cmdb_ci_storage_server';
        // -&gt; "netapp ontap 9.12.1" -&gt; "cmdb_ci_storage_server" (Storage Server).
        if (s.indexOf('printer') != -1 || s.indexOf('laserjet') != -1 || s.indexOf('jetdirect') != -1) return 'cmdb_ci_printer';
        // -&gt; "hp laserjet m507" -&gt; "cmdb_ci_printer" (Printer).
        if (s.indexOf('red hat') != -1 || s.indexOf('linux') != -1 || s.indexOf('centos') != -1 ||
            s.indexOf('ubuntu') != -1 || s.indexOf('suse') != -1 || s.indexOf('debian') != -1 ||
            s.indexOf('fedora') != -1 || s.indexOf('euleros') != -1 ||
            s.indexOf('oracle enterprise') != -1 || s.indexOf('amazon') != -1) return 'cmdb_ci_linux_server';
        // -&gt; "red hat enterprise linux 9.8", "suse linux enterprise server 15 sp5" and "amazon
        //    linux 2023" -&gt; "cmdb_ci_linux_server" (Linux Server).
        if (s.indexOf('nx-os') != -1 || s.indexOf('catos') != -1 || s.indexOf('cisco') != -1) return 'cmdb_ci_netgear';
        // -&gt; "cisco nx-os 9.3(8)" -&gt; "cmdb_ci_netgear" (Network Gear).
        return '';
        // -&gt; anything else, for example "unknown" -&gt; "" (no class).
    }

    var pref = classFor(sourcePayload.OS);
    // -&gt; pref = "cmdb_ci_esx_server" for the sample OS "VMware ESXi 7.0.3 build 24723872".
    if (!pref)
        return null;
    // -&gt; pref = "" happens for an unknown OS or a multi-guess fingerprint; this rule then declines
    //    so that rule 260 (which searches all hardware without a class) gets its turn.
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - The helper pickFqdn(table)
    // What happens : defines the helper used in the next stage. It searches one table for CIs whose
    //                fqdn field equals the scanned name (ignored classes left out), collects the
    //                sys_id of every CI found, notes which of them also carry the scanned IP
    //                address, and decides: one CI -&gt; that CI; several CIs but exactly one with the
    //                scanned IP -&gt; that one; anything else -&gt; null.
    // Why          : an FQDN is meant to be unique, but CMDBs do carry duplicates: a retired server
    //                and its rebuilt replacement, or a cluster alias loaded on two nodes. The
    //                scanned IP is the second piece of evidence that can safely break such a tie;
    //                without it the rule declines rather than guess.
    // Sample       : the ESX Server "vsdnac22xsdi004" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c) has
    //                fqdn = "vsdnac22xsdi004.sdi.corp.bankofamerica.com" -&gt; ids =
    //                ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"], ipHits =
    //                ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"] because its ip_address is also
    //                "30.206.199.36" -&gt; ids.length == 1 -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    // ---------------------------------------------------------------------------------------------
    function pickFqdn(table) {
        var gr = new GlideRecord(table);
        // -&gt; gr = a search on the table given (the class and every class beneath it); nothing has
        //    run yet
        if (!gr.isValid())
            return null;
        // -&gt; isValid() is false only when the class does not exist on this instance; then the
        //    helper declines rather than fail
        gr.addQuery('fqdn', fqdn);
        // -&gt; condition added: fqdn = "vsdnac22xsdi004.sdi.corp.bankofamerica.com" (exact match,
        //    case-insensitive)
        if (ignore)
            gr.addQuery('sys_class_name', 'NOT IN', ignore);
        // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
        //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name)
        gr.query();
        // -&gt; the search has run; gr sits before the first row of the result
        var ids = [];
        var ipHits = [];
        // -&gt; two empty lists: ids will hold every CI found, ipHits the ones that also carry the
        //    scanned IP
        while (gr.next()) {
            // -&gt; each pass of the loop looks at one CI of the result
            ids.push(gr.getUniqueValue());
            // -&gt; ids = ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"] after the first CI,
            //    ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c", "8c1d5e2f7a9b4c3d6e0f1a2b3c4d5e6f"] when a
            //    second one exists
            if (ip &amp;&amp; gr.getValue('ip_address') == ip)
                ipHits.push(gr.getUniqueValue());
            // -&gt; ipHits = ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"] when this CI's ip_address field is
            //    "30.206.199.36"; unchanged otherwise
        }
        if (ids.length == 1)
            return ids[0];
        // -&gt; exactly one CI carries the FQDN -&gt; its sys_id is the answer
        if (ids.length &gt; 1 &amp;&amp; ipHits.length == 1)
            return ipHits[0];
        // -&gt; two or more CIs carry the FQDN and exactly one of them also has the scanned IP -&gt; that
        //    one is the answer
        return null;
        // -&gt; no CI, or several CIs without a single IP confirmation -&gt; decline
    }

    // ---------------------------------------------------------------------------------------------
    // STAGE 7 - Run the helper inside the class the OS implies
    // ---------------------------------------------------------------------------------------------
    return pickFqdn(pref);
    // -&gt; pickFqdn("cmdb_ci_esx_server") -&gt; "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c" for the sample; that
    //    sys_id goes back to the framework, which links the vulnerable item to the ESX Server
    //    "vsdnac22xsdi004" and stops evaluating later rules. null makes the host continue to rule
    //    260.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;DNS&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-03 12:09:43&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;b3f9e0a493a60754e3aef0aefaba102f&lt;/sys_id&gt;&lt;sys_mod_count&gt;5&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:19&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-1668866423</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>591fd5f2938b8710e3aef0aefaba10c3</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf02d0000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM FQDN Class Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>a01fd5f2138b8710ca33c82c90c3382e</update_guid>
<update_guid_history>a01fd5f2138b8710ca33c82c90c3382e:-1668866423,9a8a597ef30b871067793c8cbbaf86fd:-2105409740,10720274b16ec7547b6c30a2fd0c7119:-1475288692</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_1bff4d7e936a4310e3aef0aefaba105f</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Combination match (name + domain evidence) across the cmdb_ci_hardware subtree for generically classed CIs. Scanned IP breaks ties.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM Hostname Domain Hardware Match&lt;/name&gt;&lt;order&gt;310&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;3&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 310 - USEM Hostname Domain Hardware Match
   =================================================================================================
   PURPOSE
   Match by short hostname plus domain evidence anywhere in the hardware tree. This is the second
   combination stage, for hosts whose CI is classed generically (Server, Computer) or differently
   from the OS.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "35884392",
     "IP": "171.128.140.192",
     "TRACKING_METHOD": "IP",
     "OS": "Ubuntu/Linux",
     "DNS": "lrche01xtrapd01.sdi.corp.bankofamerica.com"
   }

   sourceValue   = the DNS field of that record -&gt; "lrche01xtrapd01.sdi.corp.bankofamerica.com"
   sourcePayload = the whole record above; the rule also reads the IP from it
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the hardware CI named "lrche01xtrapd01" whose fqdn is
   "lrche01xtrapd01.sdi.corp.bankofamerica.com" or whose dns_domain is "sdi.corp.bankofamerica.com",
   for example a CI in the generic Server class.

   WHY THIS RULE SITS AT ORDER 310
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 300 tried hostname plus domain inside the class the OS implies (Linux Server for
                 the sample OS "Ubuntu/Linux").
   - Reaches it: hosts whose CI is classed generically (Server, Computer) or differently from the
                 scanned OS, so the class-scoped search found nothing.
   - After it  : 350 (layered DNS records) and 400/410 (short hostname without domain evidence).

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a DNS name
    2. Clean the DNS name and split it into hostname and domain
    3. Note the scanned IP address for tie-breaks
    4. Read the list of CI classes that must never be matched
    5. The helper pickCombo(table)
    6. Run the helper on the whole hardware tree
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a DNS name
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "lrche01xtrapd01.sdi.corp.bankofamerica.com" -&gt; not empty -&gt; the
    //                rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the DNS name and split it into hostname and domain
    // What happens : '' + sourceValue turns the value into plain text, trim() removes blanks at
    //                both ends, toLowerCase() lowers it. indexOf(".") finds the first dot; the text
    //                before it is the short hostname and the text after it is the domain.
    // Why          : the rule needs both halves: the hostname to find candidate CIs by name, and
    //                the domain to confirm that a candidate really belongs to the scanned domain. A
    //                name without a dot has no domain to confirm, so the rule declines and leaves
    //                it to the hostname rules (400 and above).
    // Sample       : full = "lrche01xtrapd01.sdi.corp.bankofamerica.com"; dot = 15; host =
    //                "lrche01xtrapd01"; domain = "sdi.corp.bankofamerica.com".
    // ---------------------------------------------------------------------------------------------
    var full = ('' + sourceValue).trim().toLowerCase();
    // -&gt; full = "lrche01xtrapd01.sdi.corp.bankofamerica.com".
    var dot = full.indexOf('.');
    // -&gt; dot = 15, the position of the first dot (positions count from 0).
    if (dot &lt; 1)
        return null;
    // -&gt; dot = -1 (no dot) or 0 (the name starts with a dot) -&gt; decline; 15 -&gt; carry on.
    var host = full.substring(0, dot);
    // -&gt; host = "lrche01xtrapd01" (everything before the first dot).
    var domain = full.substring(dot + 1);
    // -&gt; domain = "sdi.corp.bankofamerica.com" (everything after the first dot).
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Note the scanned IP address for tie-breaks
    // What happens : copies the IP field of the payload into plain text, or an empty text when the
    //                payload has none.
    // Why          : the IP is not used to search; it is only used later to choose between two CIs
    //                that both carry the scanned name.
    // Sample       : ip = "171.128.140.192".
    // ---------------------------------------------------------------------------------------------
    var ip = sourcePayload.IP ? '' + sourcePayload.IP : '';
    // -&gt; ip = "171.128.140.192" (or "" when the payload has no IP field).
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - The helper pickCombo(table)
    // What happens : defines the helper used in the next stage. It searches one table for CIs whose
    //                name is the short hostname, and keeps a CI only when the CI's own domain
    //                information agrees with the scanned domain: its fqdn equals the scanned name,
    //                or its dns_domain equals the scanned domain, or its fqdn starts with the
    //                hostname and contains the scanned domain. Then it decides: one confirmed CI -&gt;
    //                that CI; several but exactly one with the scanned IP -&gt; that one; anything
    //                else -&gt; null.
    // Why          : the same short hostname can exist in several domains (a test and a production
    //                machine both called app01). Requiring domain evidence on the CI itself stops
    //                the findings from landing on the namesake in another domain.
    // Sample       : the Server "lrche01xtrapd01" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c) has
    //                dns_domain = "sdi.corp.bankofamerica.com" -&gt; confirmed -&gt; good =
    //                ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"]; a CI "lrche01xtrapd01" with dns_domain =
    //                "lab.example.net" is skipped -&gt; good.length == 1 -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    // ---------------------------------------------------------------------------------------------
    function pickCombo(table) {
        var gr = new GlideRecord(table);
        // -&gt; gr = a search on the table given (the class and every class beneath it)
        if (!gr.isValid())
            return null;
        // -&gt; the class does not exist on this instance -&gt; decline rather than fail
        gr.addQuery('name', host);
        // -&gt; condition added: name = "lrche01xtrapd01" (CMDB names compare case-insensitively, so
        //    "LRCHE01XTRAPD01" is found too)
        if (ignore)
            gr.addQuery('sys_class_name', 'NOT IN', ignore);
        // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
        //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name)
        gr.query();
        // -&gt; the search has run; every CI named "lrche01xtrapd01" in the table is in the result
        var good = [];
        var ipHits = [];
        // -&gt; two empty lists: good will hold the CIs whose domain evidence agrees, ipHits those of
        //    them that also carry the scanned IP
        while (gr.next()) {
            // -&gt; each pass of the loop looks at one CI named "lrche01xtrapd01"
            var cifqdn = ('' + gr.getValue('fqdn')).toLowerCase();
            // -&gt; cifqdn = the CI's own fqdn in lower case, for example
            //    "lrche01xtrapd01.sdi.corp.bankofamerica.com", or "" when the field is empty
            var cidom = ('' + gr.getValue('dns_domain')).toLowerCase();
            // -&gt; cidom = the CI's own dns_domain in lower case, for example
            //    "sdi.corp.bankofamerica.com", or "" when empty
            if (cifqdn == full || cidom == domain ||
                (cifqdn &amp;&amp; cifqdn.indexOf(host + '.') == 0 &amp;&amp; cifqdn.indexOf(domain) &gt; 0)) {
                // -&gt; the CI confirms the domain in one of three ways: its fqdn is the scanned name;
                //    its dns_domain is the scanned domain; or its fqdn starts with
                //    "lrche01xtrapd01." and contains "sdi.corp.bankofamerica.com" somewhere after
                //    it.
                good.push(gr.getUniqueValue());
                // -&gt; good = ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"] after the first confirmed CI
                if (ip &amp;&amp; gr.getValue('ip_address') == ip)
                    ipHits.push(gr.getUniqueValue());
                // -&gt; ipHits = ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"] when this CI's ip_address is
                //    also "171.128.140.192"
            }
            // -&gt; a CI named "lrche01xtrapd01" whose domain evidence disagrees (another domain, or
            //    no domain at all) is simply skipped
        }
        if (good.length == 1)
            return good[0];
        // -&gt; exactly one CI has the name and the domain -&gt; its sys_id is the answer
        if (good.length &gt; 1 &amp;&amp; ipHits.length == 1)
            return ipHits[0];
        // -&gt; several confirmed CIs, and exactly one of them also carries the scanned IP -&gt; that one
        //    is the answer
        return null;
        // -&gt; no confirmed CI, or several without a single IP confirmation -&gt; decline
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Run the helper on the whole hardware tree
    // ---------------------------------------------------------------------------------------------
    return pickCombo('cmdb_ci_hardware');
    // -&gt; pickCombo("cmdb_ci_hardware") searches Hardware and every class beneath it -&gt;
    //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c" for the sample; null makes the host continue to rule
    //    350.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;DNS&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:42:02&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;1bff4d7e936a4310e3aef0aefaba105f&lt;/sys_id&gt;&lt;sys_mod_count&gt;2&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:19&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-1541674410</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>5d1fd5f2938b8710e3aef0aefaba10bf</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf0f00000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM Hostname Domain Hardware Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>a01fd5f29c8b87106c13bd2e96b4283a</update_guid>
<update_guid_history>a01fd5f29c8b87106c13bd2e96b4283a:-1541674410,968a997eda0b8710260c158ca5b61044:986622066</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_1fff4d7e936a4310e3aef0aefaba1068</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Last-resort IP matching, stage 2: unique owner across cmdb_ci_hardware; load-balancer VIPs and OS-contradicting owners rejected.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM IP Hardware Match&lt;/name&gt;&lt;order&gt;705&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;3&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 705 - USEM IP Hardware Match
   =================================================================================================
   PURPOSE
   Match by IP address anywhere in the hardware tree, with two extra safety checks: the CI must not
   be a load balancer, and its class must not contradict the scanned OS.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "83047621",
     "IP": "30.162.178.21",
     "TRACKING_METHOD": "IP",
     "OS": "VMware ESXi 7.0.3 build 24723872"
   }

   sourceValue   = the IP field of that record -&gt; "30.162.178.21"
   sourcePayload = the whole record above; the rule also reads the OS from it
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the one hardware CI whose ip_address is "30.162.178.21",
   accepted when it is an ESX Server or generically classed; rejected when it is a Load Balancer (a
   virtual address answered on behalf of pool members) or a CI of a contradicting class such as
   Windows Server.

   WHY THIS RULE SITS AT ORDER 705
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 700 required the address to belong to one CI of the OS-implied class.
   - Reaches it: DNS-less hosts whose CI is classed generically (Server) or whose OS gave no class.
   - After it  : 730 (address on a network adapter) and 740 (layered IP Address records).

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent an IP address
    2. Clean the address and reject addresses that identify nothing
    3. Read the list of CI classes that must never be matched
    4. Work out the CMDB class from the scanned operating system (a preference, not a requirement)
    5. List the classes that never contradict the scan
    6. The helper isLoadBalancer(id)
    7. Search the whole hardware tree for the address
    8. Require exactly one owner
    9. Reject a load balancer
   10. Reject an owner whose class contradicts the scanned OS
   11. Return the owner
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent an IP address
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "30.162.178.21" -&gt; not empty -&gt; the rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the address and reject addresses that identify nothing
    // What happens : '' + sourceValue turns the value into plain text and trim() removes blanks at
    //                both ends. Then the rule refuses loopback addresses (127.x.x.x) and link-local
    //                addresses (169.254.x.x).
    // Why          : every machine answers on 127.0.0.1, and 169.254.x.x addresses are
    //                self-assigned when no network is available, so neither can ever point at one
    //                CI.
    // Sample       : ip = "30.162.178.21" -&gt; neither prefix matches -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    var ip = ('' + sourceValue).trim();
    // -&gt; ip = "30.162.178.21".
    if (!ip || ip.indexOf('127.') == 0 || ip.indexOf('169.254.') == 0)
        return null;
    // -&gt; indexOf("127.") == 0 would mean the text starts with "127."; for "30.162.178.21" both
    //    indexOf calls return -1 -&gt; the condition is false -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Work out the CMDB class from the scanned operating system
    // What happens : the helper classFor() reads the operating system text Qualys reports and
    //                returns the CMDB class such a machine is stored in. It lowers the text, gives
    //                up when the text is an unauthenticated multi-guess fingerprint (three or more
    //                guesses separated by "/"), then looks for key words in a fixed order and
    //                returns an empty string when nothing fits. Searching a class also searches
    //                every class beneath it, so cmdb_ci_linux_server covers all Linux flavour
    //                classes.
    // Why          : evidence is only trusted when it lands in a class that agrees with the scanned
    //                OS: a Red Hat host must resolve to a Linux Server CI, never to a Windows
    //                Server that happens to carry the same value.
    // Sample       : classFor("VMware ESXi 7.0.3 build 24723872"): s = "vmware esxi 7.0.3 build
    //                24723872"; s.split("/") has 1 part; "esx" is found straight away -&gt; returns
    //                "cmdb_ci_esx_server" (ESX Server).
    // ---------------------------------------------------------------------------------------------
    function classFor(os) {
        if (!os) return '';
        // -&gt; no OS text at all -&gt; "" (no class).
        var s = ('' + os).toLowerCase();
        // -&gt; s = the OS text in lower case, so that "Windows", "WINDOWS" and "windows" all compare
        //    the same.
        if (s.split('/').length &gt; 2) return '';
        // -&gt; "ubuntu / tiny core linux / linux 2.6.x / ibm asm / hp storeonce / f5 networks big-ip
        //    / cisco ios software" splits into 7 parts at the "/" characters -&gt; more than 2 -&gt; an
        //    unauthenticated guess list -&gt; "" (no class).
        if (s.indexOf('esx') != -1) return 'cmdb_ci_esx_server';
        // -&gt; "vmware esxi 7.0.3 build 24723872" contains "esx" -&gt; "cmdb_ci_esx_server" (ESX
        //    Server).
        if (s.indexOf('windows') != -1)
            return s.indexOf('server') != -1 ? 'cmdb_ci_win_server' : 'cmdb_ci_computer';
        // -&gt; "windows server 2016 standard 64 bit edition version 1607" -&gt; "cmdb_ci_win_server"
        //    (Windows Server); "windows 10 enterprise 64 bit edition version 22h2" has no "server"
        //    -&gt; "cmdb_ci_computer" (Computer, a workstation).
        if (s.indexOf('aix') != -1) return 'cmdb_ci_aix_server';
        // -&gt; "aix 7.3 tl3" -&gt; "cmdb_ci_aix_server" (AIX Server).
        if (s.indexOf('solaris') != -1 || s.indexOf('sunos') != -1) return 'cmdb_ci_solaris_server';
        // -&gt; "oracle solaris 11.4" -&gt; "cmdb_ci_solaris_server" (Solaris Server).
        if (s.indexOf('hp-ux') != -1) return 'cmdb_ci_hpux_server';
        // -&gt; "hp-ux b.11.31" -&gt; "cmdb_ci_hpux_server" (HPUX Server).
        if (s.indexOf('netapp') != -1 || s.indexOf('ontap') != -1) return 'cmdb_ci_storage_server';
        // -&gt; "netapp ontap 9.12.1" -&gt; "cmdb_ci_storage_server" (Storage Server).
        if (s.indexOf('printer') != -1 || s.indexOf('laserjet') != -1 || s.indexOf('jetdirect') != -1) return 'cmdb_ci_printer';
        // -&gt; "hp laserjet m507" -&gt; "cmdb_ci_printer" (Printer).
        if (s.indexOf('red hat') != -1 || s.indexOf('linux') != -1 || s.indexOf('centos') != -1 ||
            s.indexOf('ubuntu') != -1 || s.indexOf('suse') != -1 || s.indexOf('debian') != -1 ||
            s.indexOf('fedora') != -1 || s.indexOf('euleros') != -1 ||
            s.indexOf('oracle enterprise') != -1 || s.indexOf('amazon') != -1) return 'cmdb_ci_linux_server';
        // -&gt; "red hat enterprise linux 9.8", "suse linux enterprise server 15 sp5" and "amazon
        //    linux 2023" -&gt; "cmdb_ci_linux_server" (Linux Server).
        if (s.indexOf('nx-os') != -1 || s.indexOf('catos') != -1 || s.indexOf('cisco') != -1) return 'cmdb_ci_netgear';
        // -&gt; "cisco nx-os 9.3(8)" -&gt; "cmdb_ci_netgear" (Network Gear).
        return '';
        // -&gt; anything else, for example "unknown" -&gt; "" (no class).
    }

    var pref = classFor(sourcePayload.OS);
    // -&gt; pref = "cmdb_ci_esx_server" for the sample OS "VMware ESXi 7.0.3 build 24723872".
    // -&gt; an empty pref does not stop this rule: the search below runs without a class, and stage 10
    //    simply has nothing to check.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - List the classes that never contradict the scan
    // What happens : lists the classes that say nothing about the operating system: Hardware,
    //                Computer, Server and UNIX Server. A CI stored in one of these never
    //                contradicts the scan.
    // Why          : many CIs are loaded into the generic Server class before discovery refines
    //                them; rejecting those would leave a large part of the estate unmatched for no
    //                good reason.
    // Sample       : generic["cmdb_ci_server"] = 1 (true), generic["cmdb_ci_win_server"] =
    //                undefined (false).
    // ---------------------------------------------------------------------------------------------
    var generic = {cmdb_ci_hardware: 1, cmdb_ci_computer: 1, cmdb_ci_server: 1,
        cmdb_ci_unix_server: 1};
    // -&gt; generic = a lookup table with the four class names; generic[cls] answers "is this class
    //    generic?".
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - The helper isLoadBalancer(id)
    // What happens : defines a small helper used later: it reports true when the CI with the given
    //                sys_id is stored in the Load Balancer class (cmdb_ci_lb) or one of its
    //                sub-classes.
    // Why          : a load balancer answers on virtual addresses on behalf of the pool members
    //                behind it; a scanned address that belongs to such a virtual IP describes a
    //                pool member, not the balancer, so the balancer must never be returned.
    // Sample       : isLoadBalancer("8c1d5e2f7a9b4c3d6e0f1a2b3c4d5e6f") = true when that sys_id is
    //                the Load Balancer "lb-sdi-core-01"; false for the ESX Server
    //                "vsdnac22xsdi009".
    // ---------------------------------------------------------------------------------------------
    function isLoadBalancer(id) {
        var lb = new GlideRecord('cmdb_ci_lb');
        // -&gt; lb = a search on the Load Balancer class; isValid() is false when that class is not
        //    installed
        return lb.isValid() &amp;&amp; lb.get(id);
        // -&gt; get(id) returns true only when a load balancer with that sys_id exists
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 7 - Search the whole hardware tree for the address
    // What happens : opens a search on cmdb_ci_hardware, keeps only CIs whose ip_address field
    //                equals the scanned address, and leaves out every class on the ignore list.
    // Why          : the class-scoped search of rule 700 found nothing, so the address is now
    //                searched everywhere; the safety comes from the three checks that follow.
    // Sample       : the search on cmdb_ci_hardware for ip_address = "30.162.178.21" finds the
    //                Server "vsdnac22xsdi009" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c, class
    //                cmdb_ci_server).
    // ---------------------------------------------------------------------------------------------
    var gr = new GlideRecord('cmdb_ci_hardware');
    // -&gt; gr = a search on Hardware and every class beneath it.
    gr.addQuery('ip_address', ip);
    // -&gt; condition added: ip_address = "30.162.178.21".
    if (ignore)
        gr.addQuery('sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
    //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    gr.query();
    // -&gt; the search has run.
    // ---------------------------------------------------------------------------------------------
    // STAGE 8 - Require exactly one owner
    // What happens : reads the first CI, remembers its sys_id and its class, and declines when a
    //                second CI carries the same address.
    // Why          : an address answered by several CIs (a shared virtual IP, an address reused
    //                after a rebuild) is never a safe match.
    // Sample       : one row -&gt; id = "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c", cls = "cmdb_ci_server".
    //                Two rows -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    if (!gr.next())
        return null;
    // -&gt; no CI carries the address -&gt; decline; the host continues to rule 730.
    var id = gr.getUniqueValue();
    // -&gt; id = "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    var cls = '' + gr.getValue('sys_class_name');
    // -&gt; cls = "cmdb_ci_server" (the exact class of the CI found).
    if (gr.hasNext())
        return null;
    // -&gt; a second CI with the same address is waiting in the result -&gt; ambiguous -&gt; decline.
    // ---------------------------------------------------------------------------------------------
    // STAGE 9 - Reject a load balancer
    // What happens : asks the helper whether the single owner is a Load Balancer CI and declines
    //                when it is.
    // Why          : a scanned address that belongs to a virtual IP describes a pool member behind
    //                the balancer, not the balancer; the findings must not land on the balancer.
    // Sample       : isLoadBalancer("3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c") = false for the Server
    //                "vsdnac22xsdi009" -&gt; carry on; true for the Load Balancer "lb-sdi-core-01" -&gt;
    //                return null.
    // ---------------------------------------------------------------------------------------------
    if (isLoadBalancer(id))
        return null;
    // -&gt; for the sample the helper answers false and nothing happens.
    // ---------------------------------------------------------------------------------------------
    // STAGE 10 - Reject an owner whose class contradicts the scanned OS
    // What happens : when the OS gave a class preference, checks that the CI found sits inside that
    //                class (or one of its sub-classes) or is generically classed; any other class
    //                is a contradiction and the rule declines.
    // Why          : an ESXi scan that lands by address on a Windows Server CI is a reused address,
    //                not the same machine.
    // Sample       : pref = "cmdb_ci_esx_server"; chk.get("3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c") on
    //                the ESX Server class is false because the CI is a plain Server, but
    //                generic["cmdb_ci_server"] is true -&gt; accepted. For a Windows Server CI both
    //                checks fail -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    if (pref) {
        var chk = new GlideRecord(pref);
        // -&gt; chk = a search on the ESX Server class and its sub-classes.
        if (!(chk.isValid() &amp;&amp; chk.get(id)) &amp;&amp; !generic[cls])
            return null;
        // -&gt; chk.get(id) is true when the CI is an ESX Server; generic[cls] is true when it is
        //    generically classed; when neither holds the CI contradicts the scan -&gt; decline.
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 11 - Return the owner
    // ---------------------------------------------------------------------------------------------
    return id;
    // -&gt; the sys_id "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c" goes back to the framework, which links the
    //    vulnerable item to the CI and stops evaluating later rules.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;IP&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:42:02&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;1fff4d7e936a4310e3aef0aefaba1068&lt;/sys_id&gt;&lt;sys_mod_count&gt;2&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:20&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-895666955</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>911fd5f2938b8710e3aef0aefaba10c0</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf3420000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM IP Hardware Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>fc1fd5f23a8b871080c5b1f2c5bd1a51</update_guid>
<update_guid_history>fc1fd5f23a8b871080c5b1f2c5bd1a51:-895666955,1e8a997e2a0b871020e8aee6a0ffab61:-804499763</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_77f9e0a493a60754e3aef0aefaba1040</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Last-resort IP matching, stage 1: address must belong to exactly one CI of the OS-implied class.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM IP Class Match&lt;/name&gt;&lt;order&gt;700&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;9&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 700 - USEM IP Class Match
   =================================================================================================
   PURPOSE
   Match by IP address, inside the class the OS implies. An address is the least trustworthy
   identifier (addresses move between machines and are shared by load balancers), so it is used only
   when the host has no serial and no usable name, and only when exactly one CI of the agreed class
   carries it.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "83047621",
     "IP": "30.162.178.21",
     "TRACKING_METHOD": "IP",
     "OS": "VMware ESXi 7.0.3 build 24723872"
   }

   sourceValue   = the IP field of that record -&gt; "30.162.178.21"
   sourcePayload = the whole record above; the rule also reads the OS from it
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the ESX Server CI whose ip_address is "30.162.178.21", for
   example "vsdnac22xsdi009"; a load balancer or a Windows CI with the same address is outside the
   class and cannot be picked.

   WHY THIS RULE SITS AT ORDER 700
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : all serial and name rules (175 to 450). The sample has no DNS name at all, so every
                 name rule declined.
   - Reaches it: DNS-less hosts, typically ESXi management interfaces and appliances (89 of the
                 21,000 hosts in the reference extract).
   - After it  : 705 searches the address across all hardware with extra safety checks; 730 and 740
                 resolve it through adapters and the layered IP records.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent an IP address
    2. Clean the address and reject addresses that identify nothing
    3. Read the list of CI classes that must never be matched
    4. Work out the CMDB class from the scanned operating system
    5. Search that class for the address
    6. Decide: exactly one CI, or decline
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent an IP address
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "30.162.178.21" -&gt; not empty -&gt; the rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the address and reject addresses that identify nothing
    // What happens : '' + sourceValue turns the value into plain text and trim() removes blanks at
    //                both ends. Then the rule refuses loopback addresses (127.x.x.x) and link-local
    //                addresses (169.254.x.x).
    // Why          : every machine answers on 127.0.0.1, and 169.254.x.x addresses are
    //                self-assigned when no network is available, so neither can ever point at one
    //                CI.
    // Sample       : ip = "30.162.178.21" -&gt; neither prefix matches -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    var ip = ('' + sourceValue).trim();
    // -&gt; ip = "30.162.178.21".
    if (!ip || ip.indexOf('127.') == 0 || ip.indexOf('169.254.') == 0)
        return null;
    // -&gt; indexOf("127.") == 0 would mean the text starts with "127."; for "30.162.178.21" both
    //    indexOf calls return -1 -&gt; the condition is false -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Work out the CMDB class from the scanned operating system
    // What happens : the helper classFor() reads the operating system text Qualys reports and
    //                returns the CMDB class such a machine is stored in. It lowers the text, gives
    //                up when the text is an unauthenticated multi-guess fingerprint (three or more
    //                guesses separated by "/"), then looks for key words in a fixed order and
    //                returns an empty string when nothing fits. Searching a class also searches
    //                every class beneath it, so cmdb_ci_linux_server covers all Linux flavour
    //                classes.
    // Why          : evidence is only trusted when it lands in a class that agrees with the scanned
    //                OS: a Red Hat host must resolve to a Linux Server CI, never to a Windows
    //                Server that happens to carry the same value.
    // Sample       : classFor("VMware ESXi 7.0.3 build 24723872"): s = "vmware esxi 7.0.3 build
    //                24723872"; s.split("/") has 1 part; "esx" is found straight away -&gt; returns
    //                "cmdb_ci_esx_server" (ESX Server).
    // ---------------------------------------------------------------------------------------------
    function classFor(os) {
        if (!os) return '';
        // -&gt; no OS text at all -&gt; "" (no class).
        var s = ('' + os).toLowerCase();
        // -&gt; s = the OS text in lower case, so that "Windows", "WINDOWS" and "windows" all compare
        //    the same.
        if (s.split('/').length &gt; 2) return '';
        // -&gt; "ubuntu / tiny core linux / linux 2.6.x / ibm asm / hp storeonce / f5 networks big-ip
        //    / cisco ios software" splits into 7 parts at the "/" characters -&gt; more than 2 -&gt; an
        //    unauthenticated guess list -&gt; "" (no class).
        if (s.indexOf('esx') != -1) return 'cmdb_ci_esx_server';
        // -&gt; "vmware esxi 7.0.3 build 24723872" contains "esx" -&gt; "cmdb_ci_esx_server" (ESX
        //    Server).
        if (s.indexOf('windows') != -1)
            return s.indexOf('server') != -1 ? 'cmdb_ci_win_server' : 'cmdb_ci_computer';
        // -&gt; "windows server 2016 standard 64 bit edition version 1607" -&gt; "cmdb_ci_win_server"
        //    (Windows Server); "windows 10 enterprise 64 bit edition version 22h2" has no "server"
        //    -&gt; "cmdb_ci_computer" (Computer, a workstation).
        if (s.indexOf('aix') != -1) return 'cmdb_ci_aix_server';
        // -&gt; "aix 7.3 tl3" -&gt; "cmdb_ci_aix_server" (AIX Server).
        if (s.indexOf('solaris') != -1 || s.indexOf('sunos') != -1) return 'cmdb_ci_solaris_server';
        // -&gt; "oracle solaris 11.4" -&gt; "cmdb_ci_solaris_server" (Solaris Server).
        if (s.indexOf('hp-ux') != -1) return 'cmdb_ci_hpux_server';
        // -&gt; "hp-ux b.11.31" -&gt; "cmdb_ci_hpux_server" (HPUX Server).
        if (s.indexOf('netapp') != -1 || s.indexOf('ontap') != -1) return 'cmdb_ci_storage_server';
        // -&gt; "netapp ontap 9.12.1" -&gt; "cmdb_ci_storage_server" (Storage Server).
        if (s.indexOf('printer') != -1 || s.indexOf('laserjet') != -1 || s.indexOf('jetdirect') != -1) return 'cmdb_ci_printer';
        // -&gt; "hp laserjet m507" -&gt; "cmdb_ci_printer" (Printer).
        if (s.indexOf('red hat') != -1 || s.indexOf('linux') != -1 || s.indexOf('centos') != -1 ||
            s.indexOf('ubuntu') != -1 || s.indexOf('suse') != -1 || s.indexOf('debian') != -1 ||
            s.indexOf('fedora') != -1 || s.indexOf('euleros') != -1 ||
            s.indexOf('oracle enterprise') != -1 || s.indexOf('amazon') != -1) return 'cmdb_ci_linux_server';
        // -&gt; "red hat enterprise linux 9.8", "suse linux enterprise server 15 sp5" and "amazon
        //    linux 2023" -&gt; "cmdb_ci_linux_server" (Linux Server).
        if (s.indexOf('nx-os') != -1 || s.indexOf('catos') != -1 || s.indexOf('cisco') != -1) return 'cmdb_ci_netgear';
        // -&gt; "cisco nx-os 9.3(8)" -&gt; "cmdb_ci_netgear" (Network Gear).
        return '';
        // -&gt; anything else, for example "unknown" -&gt; "" (no class).
    }

    var pref = classFor(sourcePayload.OS);
    // -&gt; pref = "cmdb_ci_esx_server" for the sample OS "VMware ESXi 7.0.3 build 24723872".
    if (!pref)
        return null;
    // -&gt; pref = "" happens for an unknown OS or a multi-guess fingerprint; this rule then declines
    //    so that rule 705 (which searches all hardware without a class) gets its turn.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Search that class for the address
    // What happens : opens a search on the class chosen above (and its sub-classes), keeps only CIs
    //                whose ip_address field equals the scanned address, and leaves out every class
    //                on the ignore list.
    // Why          : inside the agreed class an address is reasonably safe: a virtual IP of a load
    //                balancer, or a Windows machine that inherited the address, sits outside the
    //                ESX Server class and never appears.
    // Sample       : the search on cmdb_ci_esx_server for ip_address = "30.162.178.21" finds the
    //                ESX Server "vsdnac22xsdi009" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c).
    // ---------------------------------------------------------------------------------------------
    var gr = new GlideRecord(pref);
    // -&gt; gr = a search on the ESX Server class and every class beneath it.
    if (!gr.isValid())
        return null;
    // -&gt; the class does not exist on this instance -&gt; decline rather than fail.
    gr.addQuery('ip_address', ip);
    // -&gt; condition added: ip_address = "30.162.178.21" (exact match).
    if (ignore)
        gr.addQuery('sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
    //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Decide: exactly one CI, or decline
    // What happens : runs the search, reads the first CI and accepts it only when there is no
    //                second CI in the result.
    // Why          : every finding of this host will be linked to the CI the rule returns. A wrong
    //                CI sends the findings to the wrong owner, so when two CIs share the value the
    //                rule refuses to guess and answers null; a later rule with different evidence
    //                may still resolve the host, and if none does the host stays unmatched for the
    //                CMDB team to review.
    // Sample       : one CI -&gt; the ESX Server "vsdnac22xsdi009" -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". No CI -&gt; return null and rule 705 gets its
    //                turn. Two CIs -&gt; two ESX Servers carrying "30.162.178.21" (an address reused
    //                after a rebuild) -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    gr.query();
    // -&gt; the search has run; gr now sits just before the first row of the result (0, 1 or more
    //    CIs).
    if (!gr.next())
        return null;
    // -&gt; next() moves to the first CI and returns true. When the result is empty it returns false
    //    and the rule declines with null.
    var match = gr.getUniqueValue();
    // -&gt; match = the 32-character sys_id of that CI, for example
    //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"; this is the value the framework expects back.
    if (gr.hasNext())
        return null;
    // -&gt; hasNext() is true when a second CI is waiting in the result: two owners of one value is an
    //    ambiguity, so the rule declines.
    return match;
    // -&gt; exactly one CI: its sys_id goes back to the framework, which links the vulnerable item to
    //    it and stops evaluating later rules.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;IP&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-03 12:09:43&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;77f9e0a493a60754e3aef0aefaba1040&lt;/sys_id&gt;&lt;sys_mod_count&gt;9&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:20&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-1187169003</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>951fd5f2938b8710e3aef0aefaba10c1</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf2fe0000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM IP Class Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>301fd5f2db8b871043f08a5ec2e3aa4e</update_guid>
<update_guid_history>301fd5f2db8b871043f08a5ec2e3aa4e:-1187169003,9e8a997e660b87108240c5d6c320295e:1623998361,90720274f56ec7544297f09c3f85a51e:36643599</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_97ff4d7e936a4310e3aef0aefaba106d</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Last-resort IP matching, stage 3: address resolved through the network adapter to a single owning CI; VIPs rejected.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM IP Adapter Match&lt;/name&gt;&lt;order&gt;730&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;3&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 730 - USEM IP Adapter Match
   =================================================================================================
   PURPOSE
   Match by an IP address recorded on a Network Adapter record rather than on the CI itself
   (multi-homed servers, discovery-populated CIs). The adapter leads to its owning CI.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "83047621",
     "IP": "30.162.178.21",
     "TRACKING_METHOD": "IP",
     "OS": "VMware ESXi 7.0.3 build 24723872"
   }

   sourceValue   = the IP field of that record -&gt; "30.162.178.21"
   sourcePayload = the whole record above
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the CI that owns the adapter carrying "30.162.178.21", for
   example adapter "vmk0" of the ESX Server "vsdnac22xsdi009"; declined when adapters of two
   different CIs carry the address or when the owner is a load balancer.

   WHY THIS RULE SITS AT ORDER 730
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 700/705 looked for the address in the ip_address field of the CI record itself.
   - Reaches it: hosts whose address is stored on an adapter record only.
   - After it  : 740 (layered IP Address records) and then the last-resort name rule 850.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent an IP address
    2. Clean the address and reject addresses that identify nothing
    3. Read the list of CI classes that must never be matched
    4. The helper isLoadBalancer(id)
    5. Search the Network Adapter records for the address
    6. Collect the owning CIs
    7. Decide: one owner that is not a load balancer, or decline
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent an IP address
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "30.162.178.21" -&gt; not empty -&gt; the rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the address and reject addresses that identify nothing
    // What happens : '' + sourceValue turns the value into plain text and trim() removes blanks at
    //                both ends. Then the rule refuses loopback addresses (127.x.x.x) and link-local
    //                addresses (169.254.x.x).
    // Why          : every machine answers on 127.0.0.1, and 169.254.x.x addresses are
    //                self-assigned when no network is available, so neither can ever point at one
    //                CI.
    // Sample       : ip = "30.162.178.21" -&gt; neither prefix matches -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    var ip = ('' + sourceValue).trim();
    // -&gt; ip = "30.162.178.21".
    if (!ip || ip.indexOf('127.') == 0 || ip.indexOf('169.254.') == 0)
        return null;
    // -&gt; indexOf("127.") == 0 would mean the text starts with "127."; for "30.162.178.21" both
    //    indexOf calls return -1 -&gt; the condition is false -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - The helper isLoadBalancer(id)
    // What happens : defines a small helper used later: it reports true when the CI with the given
    //                sys_id is stored in the Load Balancer class (cmdb_ci_lb) or one of its
    //                sub-classes.
    // Why          : a load balancer answers on virtual addresses on behalf of the pool members
    //                behind it; a scanned address that belongs to such a virtual IP describes a
    //                pool member, not the balancer, so the balancer must never be returned.
    // Sample       : isLoadBalancer("8c1d5e2f7a9b4c3d6e0f1a2b3c4d5e6f") = true when that sys_id is
    //                the Load Balancer "lb-sdi-core-01"; false for the ESX Server
    //                "vsdnac22xsdi009".
    // ---------------------------------------------------------------------------------------------
    function isLoadBalancer(id) {
        var lb = new GlideRecord('cmdb_ci_lb');
        // -&gt; lb = a search on the Load Balancer class; isValid() is false when that class is not
        //    installed
        return lb.isValid() &amp;&amp; lb.get(id);
        // -&gt; get(id) returns true only when a load balancer with that sys_id exists
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Search the Network Adapter records for the address
    // What happens : searches the Network Adapter table for adapters whose ip_address equals the
    //                scanned address and that belong to a CI whose class is not on the ignore list.
    // Why          : discovery stores one adapter record per network card of a device; a server
    //                with several cards keeps its addresses there instead of on the CI record.
    // Sample       : adapter "vmk0" with ip_address "30.162.178.21" belongs to the ESX Server
    //                "vsdnac22xsdi009" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c).
    // ---------------------------------------------------------------------------------------------
    var nic = new GlideRecord('cmdb_ci_network_adapter');
    // -&gt; nic = a search on the Network Adapter table.
    nic.addQuery('ip_address', ip);
    // -&gt; condition added: ip_address = "30.162.178.21".
    nic.addNotNullQuery('cmdb_ci');
    // -&gt; condition added: cmdb_ci is not empty, so only adapters that belong to a CI are
    //    considered.
    if (ignore)
        nic.addQuery('cmdb_ci.sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: the class of the owning CI is NOT IN (sn_sec_cmn_unmatched_ci,
    //    sn_vul_qualys_ci, cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    nic.query();
    // -&gt; the search has run; each row is one adapter carrying the address.
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Collect the owning CIs
    // What happens : walks the adapters and records each distinct owning CI once.
    // Why          : one device can have two adapters on the same address (a bonded pair); it must
    //                count as one owner, while two different devices must count as two.
    // Sample       : one adapter -&gt; owners = {"3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c": true}, count = 1,
    //                first = "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    // ---------------------------------------------------------------------------------------------
    var owners = {};
    var count = 0, first = null;
    // -&gt; owners = {} (every CI seen, keyed by sys_id), count = 0, first = null.
    while (nic.next()) {
        // -&gt; each pass of the loop looks at one adapter.
        var owner = nic.getValue('cmdb_ci');
        // -&gt; owner = the sys_id of the CI that owns this adapter, for example
        //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
        if (!owners[owner]) {
            owners[owner] = true;
            count++;
            if (count == 1)
                first = owner;
        }
        // -&gt; the first time a CI is seen: owners gains it, count grows by one, and first remembers
        //    the very first CI. A second adapter of the same CI changes nothing.
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 7 - Decide: one owner that is not a load balancer, or decline
    // What happens : accepts the single owner when exactly one CI was collected and it is not a
    //                Load Balancer; declines otherwise.
    // Why          : two owners cannot be told apart by the address, and a load balancer answering
    //                on a virtual address is not the host that was scanned.
    // Sample       : count = 1 and isLoadBalancer("3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c") = false -&gt;
    //                return "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". count = 2, or the owner is the Load
    //                Balancer "lb-sdi-core-01" -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    if (count == 1 &amp;&amp; !isLoadBalancer(first))
        return first;
    // -&gt; exactly one owner and not a load balancer -&gt; its sys_id goes back to the framework.
    return null;
    // -&gt; no owner, several owners, or a load balancer -&gt; decline; the host continues to rule 740.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;IP&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:42:02&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;97ff4d7e936a4310e3aef0aefaba106d&lt;/sys_id&gt;&lt;sys_mod_count&gt;2&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:20&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-1842880445</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>991fd5f2938b8710e3aef0aefaba10c2</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf3900000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM IP Adapter Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>bc1fd5f2768b8710e75a99cb96365755</update_guid>
<update_guid_history>bc1fd5f2768b8710e75a99cb96365755:-1842880445,5a8a997ed50b87106f5b43ca42659964:-1228599577</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_b3f9e0a493a60754e3aef0aefaba103f</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Combination match inside the OS-implied class: CI name = hostname AND fqdn/dns_domain confirms the domain. Scanned IP breaks ties.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM Hostname Domain Class Match&lt;/name&gt;&lt;order&gt;300&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;7&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 300 - USEM Hostname Domain Class Match
   =================================================================================================
   PURPOSE
   Match by the combination of short hostname and domain, inside the class the OS implies. It serves
   CIs that are named with the short hostname and carry the domain in another field (dns_domain or
   fqdn) instead of an exact fqdn value.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "35832680",
     "IP": "171.128.225.96",
     "TRACKING_METHOD": "AGENT",
     "OS": "Red Hat Enterprise Linux 9.8",
     "DNS": "ah-1047132-001.sdi.corp.bankofamerica.com",
     "QG_HOSTID": "633769a4-0139-0002-e352-005056bf41ea"
   }

   sourceValue   = the DNS field of that record -&gt; "ah-1047132-001.sdi.corp.bankofamerica.com"
   sourcePayload = the whole record above; the rule also reads the OS and the IP from it
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the Linux Server CI named "ah-1047132-001" whose dns_domain is
   "sdi.corp.bankofamerica.com" (or whose fqdn is the scanned name); a namesake in another domain is
   never picked.

   WHY THIS RULE SITS AT ORDER 300
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 250/260 looked for the exact FQDN in the fqdn field.
   - Reaches it: hosts whose CI has no exact fqdn value but is named with the short hostname and
                 shows the domain elsewhere.
   - After it  : 310 repeats the search across all hardware; 350 uses the layered DNS records;
                 400/410 accept a unique short name without any domain evidence.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a DNS name
    2. Clean the DNS name and split it into hostname and domain
    3. Note the scanned IP address for tie-breaks
    4. Read the list of CI classes that must never be matched
    5. Work out the CMDB class from the scanned operating system
    6. The helper pickCombo(table)
    7. Run the helper inside the class the OS implies
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a DNS name
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "ah-1047132-001.sdi.corp.bankofamerica.com" -&gt; not empty -&gt; the
    //                rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the DNS name and split it into hostname and domain
    // What happens : '' + sourceValue turns the value into plain text, trim() removes blanks at
    //                both ends, toLowerCase() lowers it. indexOf(".") finds the first dot; the text
    //                before it is the short hostname and the text after it is the domain.
    // Why          : the rule needs both halves: the hostname to find candidate CIs by name, and
    //                the domain to confirm that a candidate really belongs to the scanned domain. A
    //                name without a dot has no domain to confirm, so the rule declines and leaves
    //                it to the hostname rules (400 and above).
    // Sample       : full = "ah-1047132-001.sdi.corp.bankofamerica.com"; dot = 14; host =
    //                "ah-1047132-001"; domain = "sdi.corp.bankofamerica.com".
    // ---------------------------------------------------------------------------------------------
    var full = ('' + sourceValue).trim().toLowerCase();
    // -&gt; full = "ah-1047132-001.sdi.corp.bankofamerica.com".
    var dot = full.indexOf('.');
    // -&gt; dot = 14, the position of the first dot (positions count from 0).
    if (dot &lt; 1)
        return null;
    // -&gt; dot = -1 (no dot) or 0 (the name starts with a dot) -&gt; decline; 14 -&gt; carry on.
    var host = full.substring(0, dot);
    // -&gt; host = "ah-1047132-001" (everything before the first dot).
    var domain = full.substring(dot + 1);
    // -&gt; domain = "sdi.corp.bankofamerica.com" (everything after the first dot).
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Note the scanned IP address for tie-breaks
    // What happens : copies the IP field of the payload into plain text, or an empty text when the
    //                payload has none.
    // Why          : the IP is not used to search; it is only used later to choose between two CIs
    //                that both carry the scanned name.
    // Sample       : ip = "171.128.225.96".
    // ---------------------------------------------------------------------------------------------
    var ip = sourcePayload.IP ? '' + sourcePayload.IP : '';
    // -&gt; ip = "171.128.225.96" (or "" when the payload has no IP field).
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Work out the CMDB class from the scanned operating system
    // What happens : the helper classFor() reads the operating system text Qualys reports and
    //                returns the CMDB class such a machine is stored in. It lowers the text, gives
    //                up when the text is an unauthenticated multi-guess fingerprint (three or more
    //                guesses separated by "/"), then looks for key words in a fixed order and
    //                returns an empty string when nothing fits. Searching a class also searches
    //                every class beneath it, so cmdb_ci_linux_server covers all Linux flavour
    //                classes.
    // Why          : evidence is only trusted when it lands in a class that agrees with the scanned
    //                OS: a Red Hat host must resolve to a Linux Server CI, never to a Windows
    //                Server that happens to carry the same value.
    // Sample       : classFor("Red Hat Enterprise Linux 9.8"): s = "red hat enterprise linux 9.8";
    //                s.split("/") has 1 part; "esx", "windows", "aix", "solaris", "sunos", "hp-ux",
    //                "netapp", "ontap", "printer", "laserjet" and "jetdirect" are not found; "red
    //                hat" is found -&gt; returns "cmdb_ci_linux_server" (Linux Server).
    // ---------------------------------------------------------------------------------------------
    function classFor(os) {
        if (!os) return '';
        // -&gt; no OS text at all -&gt; "" (no class).
        var s = ('' + os).toLowerCase();
        // -&gt; s = the OS text in lower case, so that "Windows", "WINDOWS" and "windows" all compare
        //    the same.
        if (s.split('/').length &gt; 2) return '';
        // -&gt; "ubuntu / tiny core linux / linux 2.6.x / ibm asm / hp storeonce / f5 networks big-ip
        //    / cisco ios software" splits into 7 parts at the "/" characters -&gt; more than 2 -&gt; an
        //    unauthenticated guess list -&gt; "" (no class).
        if (s.indexOf('esx') != -1) return 'cmdb_ci_esx_server';
        // -&gt; "vmware esxi 7.0.3 build 24723872" contains "esx" -&gt; "cmdb_ci_esx_server" (ESX
        //    Server).
        if (s.indexOf('windows') != -1)
            return s.indexOf('server') != -1 ? 'cmdb_ci_win_server' : 'cmdb_ci_computer';
        // -&gt; "windows server 2016 standard 64 bit edition version 1607" -&gt; "cmdb_ci_win_server"
        //    (Windows Server); "windows 10 enterprise 64 bit edition version 22h2" has no "server"
        //    -&gt; "cmdb_ci_computer" (Computer, a workstation).
        if (s.indexOf('aix') != -1) return 'cmdb_ci_aix_server';
        // -&gt; "aix 7.3 tl3" -&gt; "cmdb_ci_aix_server" (AIX Server).
        if (s.indexOf('solaris') != -1 || s.indexOf('sunos') != -1) return 'cmdb_ci_solaris_server';
        // -&gt; "oracle solaris 11.4" -&gt; "cmdb_ci_solaris_server" (Solaris Server).
        if (s.indexOf('hp-ux') != -1) return 'cmdb_ci_hpux_server';
        // -&gt; "hp-ux b.11.31" -&gt; "cmdb_ci_hpux_server" (HPUX Server).
        if (s.indexOf('netapp') != -1 || s.indexOf('ontap') != -1) return 'cmdb_ci_storage_server';
        // -&gt; "netapp ontap 9.12.1" -&gt; "cmdb_ci_storage_server" (Storage Server).
        if (s.indexOf('printer') != -1 || s.indexOf('laserjet') != -1 || s.indexOf('jetdirect') != -1) return 'cmdb_ci_printer';
        // -&gt; "hp laserjet m507" -&gt; "cmdb_ci_printer" (Printer).
        if (s.indexOf('red hat') != -1 || s.indexOf('linux') != -1 || s.indexOf('centos') != -1 ||
            s.indexOf('ubuntu') != -1 || s.indexOf('suse') != -1 || s.indexOf('debian') != -1 ||
            s.indexOf('fedora') != -1 || s.indexOf('euleros') != -1 ||
            s.indexOf('oracle enterprise') != -1 || s.indexOf('amazon') != -1) return 'cmdb_ci_linux_server';
        // -&gt; "red hat enterprise linux 9.8", "suse linux enterprise server 15 sp5" and "amazon
        //    linux 2023" -&gt; "cmdb_ci_linux_server" (Linux Server).
        if (s.indexOf('nx-os') != -1 || s.indexOf('catos') != -1 || s.indexOf('cisco') != -1) return 'cmdb_ci_netgear';
        // -&gt; "cisco nx-os 9.3(8)" -&gt; "cmdb_ci_netgear" (Network Gear).
        return '';
        // -&gt; anything else, for example "unknown" -&gt; "" (no class).
    }

    var pref = classFor(sourcePayload.OS);
    // -&gt; pref = "cmdb_ci_linux_server" for the sample OS "Red Hat Enterprise Linux 9.8".
    if (!pref)
        return null;
    // -&gt; pref = "" happens for an unknown OS or a multi-guess fingerprint; this rule then declines
    //    so that rule 310 (which searches all hardware without a class) gets its turn.
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - The helper pickCombo(table)
    // What happens : defines the helper used in the next stage. It searches one table for CIs whose
    //                name is the short hostname, and keeps a CI only when the CI's own domain
    //                information agrees with the scanned domain: its fqdn equals the scanned name,
    //                or its dns_domain equals the scanned domain, or its fqdn starts with the
    //                hostname and contains the scanned domain. Then it decides: one confirmed CI -&gt;
    //                that CI; several but exactly one with the scanned IP -&gt; that one; anything
    //                else -&gt; null.
    // Why          : the same short hostname can exist in several domains (a test and a production
    //                machine both called app01). Requiring domain evidence on the CI itself stops
    //                the findings from landing on the namesake in another domain.
    // Sample       : the Linux Server "ah-1047132-001" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c)
    //                has dns_domain = "sdi.corp.bankofamerica.com" -&gt; confirmed -&gt; good =
    //                ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"]; a CI "ah-1047132-001" with dns_domain =
    //                "lab.example.net" is skipped -&gt; good.length == 1 -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    // ---------------------------------------------------------------------------------------------
    function pickCombo(table) {
        var gr = new GlideRecord(table);
        // -&gt; gr = a search on the table given (the class and every class beneath it)
        if (!gr.isValid())
            return null;
        // -&gt; the class does not exist on this instance -&gt; decline rather than fail
        gr.addQuery('name', host);
        // -&gt; condition added: name = "ah-1047132-001" (CMDB names compare case-insensitively, so
        //    "AH-1047132-001" is found too)
        if (ignore)
            gr.addQuery('sys_class_name', 'NOT IN', ignore);
        // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
        //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name)
        gr.query();
        // -&gt; the search has run; every CI named "ah-1047132-001" in the table is in the result
        var good = [];
        var ipHits = [];
        // -&gt; two empty lists: good will hold the CIs whose domain evidence agrees, ipHits those of
        //    them that also carry the scanned IP
        while (gr.next()) {
            // -&gt; each pass of the loop looks at one CI named "ah-1047132-001"
            var cifqdn = ('' + gr.getValue('fqdn')).toLowerCase();
            // -&gt; cifqdn = the CI's own fqdn in lower case, for example
            //    "ah-1047132-001.sdi.corp.bankofamerica.com", or "" when the field is empty
            var cidom = ('' + gr.getValue('dns_domain')).toLowerCase();
            // -&gt; cidom = the CI's own dns_domain in lower case, for example
            //    "sdi.corp.bankofamerica.com", or "" when empty
            if (cifqdn == full || cidom == domain ||
                (cifqdn &amp;&amp; cifqdn.indexOf(host + '.') == 0 &amp;&amp; cifqdn.indexOf(domain) &gt; 0)) {
                // -&gt; the CI confirms the domain in one of three ways: its fqdn is the scanned name;
                //    its dns_domain is the scanned domain; or its fqdn starts with
                //    "ah-1047132-001." and contains "sdi.corp.bankofamerica.com" somewhere after
                //    it.
                good.push(gr.getUniqueValue());
                // -&gt; good = ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"] after the first confirmed CI
                if (ip &amp;&amp; gr.getValue('ip_address') == ip)
                    ipHits.push(gr.getUniqueValue());
                // -&gt; ipHits = ["3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"] when this CI's ip_address is
                //    also "171.128.225.96"
            }
            // -&gt; a CI named "ah-1047132-001" whose domain evidence disagrees (another domain, or no
            //    domain at all) is simply skipped
        }
        if (good.length == 1)
            return good[0];
        // -&gt; exactly one CI has the name and the domain -&gt; its sys_id is the answer
        if (good.length &gt; 1 &amp;&amp; ipHits.length == 1)
            return ipHits[0];
        // -&gt; several confirmed CIs, and exactly one of them also carries the scanned IP -&gt; that one
        //    is the answer
        return null;
        // -&gt; no confirmed CI, or several without a single IP confirmation -&gt; decline
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 7 - Run the helper inside the class the OS implies
    // ---------------------------------------------------------------------------------------------
    return pickCombo(pref);
    // -&gt; pickCombo("cmdb_ci_linux_server") -&gt; "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c" for the sample;
    //    that sys_id goes back to the framework, which links the vulnerable item to the Linux
    //    Server "ah-1047132-001" and stops evaluating later rules. null makes the host continue to
    //    rule 310.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;DNS&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-03 12:09:43&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;b3f9e0a493a60754e3aef0aefaba103f&lt;/sys_id&gt;&lt;sys_mod_count&gt;6&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:19&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-1893413851</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>9d1fd5f2938b8710e3aef0aefaba10c3</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf0b60000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM Hostname Domain Class Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>e01fd5f2948b871013c072e98eb6c536</update_guid>
<update_guid_history>e01fd5f2948b871013c072e98eb6c536:-1893413851,568a997e370b8710dfaba0be1efeb403:-3713466,58720274976ec754f02435c9b0a8cd1b:257523542</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_d7ff4d7e936a4310e3aef0aefaba100b</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Serial number resolved across the cmdb_ci_hardware subtree when the OS gives no usable class. Junk serials filtered; unique owner required.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM Serial Number Hardware Match&lt;/name&gt;&lt;order&gt;180&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;3&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 180 - USEM Serial Number Hardware Match
   =================================================================================================
   PURPOSE
   Match a scanned host to its CI by serial number anywhere in the hardware tree (servers,
   computers, network gear, storage, printers). It is the second and last serial stage: it runs when
   rule 175 could not use the class the OS implies, and it still insists on the serial belonging to
   exactly one CI.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "35920204",
     "IP": "171.135.28.125",
     "TRACKING_METHOD": "IP",
     "OS": "Ubuntu / Tiny Core Linux / Linux 2.6.x / IBM ASM / HP StoreOnce / F5 Networks Big-IP / Cisco IOS Software",
     "DNS": "txr9gxcenah031.sdi.corp.bankofamerica.com",
     "SERIAL_NUMBER": "MXQ13005TC"
   }

   sourceValue   = the SERIAL_NUMBER field of that record -&gt; "MXQ13005TC"
   sourcePayload = the whole record above
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the one hardware CI whose serial_number is "MXQ13005TC", for
   example the Server CI "txr9gxcenah031" (stored in the generic Server class because the scan could
   not identify its OS); its sys_id is returned.

   WHY THIS RULE SITS AT ORDER 180
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 175 already tried the serial inside the class the OS implies.
   - Reaches it: hosts with a serial whose OS is unknown (the sample OS is an unauthenticated scan
                 listing seven guesses, so no class can be chosen) or whose CI sits in a different
                 class than the OS suggests, so that 175 found nothing.
   - After it  : 200 and above switch to names and addresses. A serial that is not unique in the
                 whole hardware tree is never used.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a serial number
    2. Clean the serial number
    3. Reject placeholder serial numbers
    4. Read the list of CI classes that must never be matched
    5. Search the whole hardware tree for the serial
    6. Decide: exactly one CI, or decline
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a serial number
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "MXQ13005TC" -&gt; not empty -&gt; the rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the serial number
    // What happens : '' + sourceValue turns the platform value into a plain text string and trim()
    //                removes blanks at both ends. Blanks inside the value stay, because they are
    //                part of the serial.
    // Why          : some feeds pad values with blanks; a search for " MXQ13005TC " would not find
    //                "MXQ13005TC".
    // Sample       : serial = "MXQ13005TC".
    // ---------------------------------------------------------------------------------------------
    var serial = ('' + sourceValue).trim();
    // -&gt; serial = "MXQ13005TC" (10 characters).
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Reject placeholder serial numbers
    // What happens : the rule keeps a list of placeholder serials that vendors ship on thousands of
    //                machines ("To Be Filled By O.E.M.", "System Serial Number", "0123456789",
    //                "None" and so on) and refuses to search for any of them, or for a serial
    //                shorter than four characters.
    // Why          : a placeholder would match dozens of CIs at once and the tie could never be
    //                broken; declining at once lets the name and address rules do the work instead.
    // Sample       : "mxq13005tc" is 10 characters long and is not in the list -&gt; carry on. "N/A"
    //                would be found as ",n/a," in the list -&gt; decline. "12" is shorter than four
    //                characters -&gt; decline.
    // ---------------------------------------------------------------------------------------------
    var junk = ',0,none,n/a,na,unknown,empty,not specified,not available,no serial,' +
        'default string,to be filled by o.e.m.,system serial number,chassis serial number,' +
        '0123456789,1234567890,';
    // -&gt; junk = one long text with a comma before and after every placeholder, so that "," + serial
    //    + "," can only be found when the whole serial equals a placeholder (",na," is never found
    //    inside ",not available,").
    if (serial.length &lt; 4 || junk.indexOf(',' + serial.toLowerCase() + ',') != -1)
        return null;
    // -&gt; serial.toLowerCase() = "mxq13005tc"; junk.indexOf(",mxq13005tc,") = -1 (not found) and the
    //    length is 10 -&gt; the condition is false -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Search the whole hardware tree for the serial
    // What happens : opens a search on cmdb_ci_hardware, the parent class of every physical and
    //                virtual device class (Server, Linux Server, Windows Server, Computer, Network
    //                Gear, Storage Server, Printer and so on), keeps only CIs whose serial_number
    //                equals the cleaned serial, and leaves out every class on the ignore list. No
    //                class preference is used at this stage.
    // Why          : the OS gave no usable class, so the serial itself has to carry the decision;
    //                that is safe because the next stage still requires the serial to belong to
    //                exactly one CI in the whole tree.
    // Sample       : the search on cmdb_ci_hardware for serial_number = "MXQ13005TC" finds the
    //                Server "txr9gxcenah031" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c).
    // ---------------------------------------------------------------------------------------------
    var gr = new GlideRecord('cmdb_ci_hardware');
    // -&gt; gr = a search on Hardware and every class beneath it; nothing has run yet.
    gr.addQuery('serial_number', serial);
    // -&gt; condition added: serial_number = "MXQ13005TC" (exact match, case-insensitive).
    if (ignore)
        gr.addQuery('sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
    //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Decide: exactly one CI, or decline
    // What happens : runs the search, reads the first CI and accepts it only when there is no
    //                second CI in the result.
    // Why          : every finding of this host will be linked to the CI the rule returns. A wrong
    //                CI sends the findings to the wrong owner, so when two CIs share the value the
    //                rule refuses to guess and answers null; a later rule with different evidence
    //                may still resolve the host, and if none does the host stays unmatched for the
    //                CMDB team to review.
    // Sample       : one CI -&gt; the Server "txr9gxcenah031" -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". No CI -&gt; return null and rule 200 gets its
    //                turn. Two CIs -&gt; "txr9gxcenah031" and a Storage Server that was loaded with
    //                the same serial -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    gr.query();
    // -&gt; the search has run; gr now sits just before the first row of the result (0, 1 or more
    //    CIs).
    if (!gr.next())
        return null;
    // -&gt; next() moves to the first CI and returns true. When the result is empty it returns false
    //    and the rule declines with null.
    var match = gr.getUniqueValue();
    // -&gt; match = the 32-character sys_id of that CI, for example
    //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"; this is the value the framework expects back.
    if (gr.hasNext())
        return null;
    // -&gt; hasNext() is true when a second CI is waiting in the result: two owners of one value is an
    //    ambiguity, so the rule declines.
    return match;
    // -&gt; exactly one CI: its sys_id goes back to the framework, which links the vulnerable item to
    //    it and stops evaluating later rules.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;SERIAL_NUMBER&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:42:02&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;d7ff4d7e936a4310e3aef0aefaba100b&lt;/sys_id&gt;&lt;sys_mod_count&gt;2&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:19&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-2134523287</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>d11fd5f2938b8710e3aef0aefaba10c4</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bef8a0000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM Serial Number Hardware Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>a01fd5f2a98b87106bf3c081ed1f1c26</update_guid>
<update_guid_history>a01fd5f2a98b87106bf3c081ed1f1c26:-2134523287,128a597e310b87106bb129eb12de1ed2:-957784403</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_23ff4d7e936a4310e3aef0aefaba1072</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Last-resort IP matching, stage 4: address resolved through the layered IP model (IP record -&amp;gt; adapter -&amp;gt; owning CI); VIPs rejected.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM IP Layered Match&lt;/name&gt;&lt;order&gt;740&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;3&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 740 - USEM IP Layered Match
   =================================================================================================
   PURPOSE
   Match by IP address through the layered model that CMDB discovery maintains: an IP Address record
   belongs to a Network Adapter, and the adapter belongs to the CI.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "83047621",
     "IP": "30.162.178.21",
     "TRACKING_METHOD": "IP",
     "OS": "VMware ESXi 7.0.3 build 24723872"
   }

   sourceValue   = the IP field of that record -&gt; "30.162.178.21"
   sourcePayload = the whole record above
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the CI at the end of the chain IP Address "30.162.178.21" -&gt;
   adapter "vmk0" -&gt; ESX Server "vsdnac22xsdi009"; declined for several owners or a load balancer.

   WHY THIS RULE SITS AT ORDER 740
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 730 looked for the address on Network Adapter records.
   - Reaches it: hosts whose address exists only as an IP Address record in the layered model.
   - After it  : 850, the last-resort broad name search; if that also declines, the out-of-box
                 Qualys rules (860 and above) get their turn.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent an IP address
    2. Clean the address and reject addresses that identify nothing
    3. Read the list of CI classes that must never be matched
    4. The helper isLoadBalancer(id)
    5. Search the IP Address records for the address
    6. Collect the owning CIs
    7. Decide: one owner that is not a load balancer, or decline
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent an IP address
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "30.162.178.21" -&gt; not empty -&gt; the rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the address and reject addresses that identify nothing
    // What happens : '' + sourceValue turns the value into plain text and trim() removes blanks at
    //                both ends. Then the rule refuses loopback addresses (127.x.x.x) and link-local
    //                addresses (169.254.x.x).
    // Why          : every machine answers on 127.0.0.1, and 169.254.x.x addresses are
    //                self-assigned when no network is available, so neither can ever point at one
    //                CI.
    // Sample       : ip = "30.162.178.21" -&gt; neither prefix matches -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    var ip = ('' + sourceValue).trim();
    // -&gt; ip = "30.162.178.21".
    if (!ip || ip.indexOf('127.') == 0 || ip.indexOf('169.254.') == 0)
        return null;
    // -&gt; indexOf("127.") == 0 would mean the text starts with "127."; for "30.162.178.21" both
    //    indexOf calls return -1 -&gt; the condition is false -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - The helper isLoadBalancer(id)
    // What happens : defines a small helper used later: it reports true when the CI with the given
    //                sys_id is stored in the Load Balancer class (cmdb_ci_lb) or one of its
    //                sub-classes.
    // Why          : a load balancer answers on virtual addresses on behalf of the pool members
    //                behind it; a scanned address that belongs to such a virtual IP describes a
    //                pool member, not the balancer, so the balancer must never be returned.
    // Sample       : isLoadBalancer("8c1d5e2f7a9b4c3d6e0f1a2b3c4d5e6f") = true when that sys_id is
    //                the Load Balancer "lb-sdi-core-01"; false for the ESX Server
    //                "vsdnac22xsdi009".
    // ---------------------------------------------------------------------------------------------
    function isLoadBalancer(id) {
        var lb = new GlideRecord('cmdb_ci_lb');
        // -&gt; lb = a search on the Load Balancer class; isValid() is false when that class is not
        //    installed
        return lb.isValid() &amp;&amp; lb.get(id);
        // -&gt; get(id) returns true only when a load balancer with that sys_id exists
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Search the IP Address records for the address
    // What happens : searches the IP Address table (cmdb_ci_ip_address) for records whose
    //                ip_address equals the scanned address and whose adapter (nic) belongs to a CI
    //                whose class is not on the ignore list. Through dot-walking, nic.cmdb_ci
    //                reaches the CI two links away.
    // Why          : newer discovery writes each address as its own record linked to the adapter;
    //                rule 730 cannot see those because the adapter record itself may carry no
    //                address.
    // Sample       : the IP Address record "30.162.178.21" belongs to adapter "vmk0", which belongs
    //                to the ESX Server "vsdnac22xsdi009" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c).
    // ---------------------------------------------------------------------------------------------
    var ipGr = new GlideRecord('cmdb_ci_ip_address');
    // -&gt; ipGr = a search on the IP Address table.
    if (!ipGr.isValid())
        return null;
    // -&gt; the layered model is not installed on this instance -&gt; decline rather than fail.
    ipGr.addQuery('ip_address', ip);
    // -&gt; condition added: ip_address = "30.162.178.21".
    ipGr.addNotNullQuery('nic.cmdb_ci');
    // -&gt; condition added: the record belongs to an adapter (nic) that belongs to a CI (cmdb_ci).
    if (ignore)
        ipGr.addQuery('nic.cmdb_ci.sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: the class of the CI at the end of the chain is NOT IN
    //    (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci, cmdb_ci_unclassed_hardware,
    //    cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    ipGr.query();
    // -&gt; the search has run; each row is one IP Address record carrying the address.
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Collect the owning CIs
    // What happens : walks the records and notes each distinct CI at the end of a chain once.
    // Why          : a device may hold the same address on two adapters; it must count as one
    //                owner, while two different devices must count as two.
    // Sample       : one record -&gt; owners = {"3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c": true}, count = 1,
    //                first = "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
    // ---------------------------------------------------------------------------------------------
    var owners = {};
    var count = 0, first = null;
    // -&gt; owners = {} (every CI seen, keyed by sys_id), count = 0, first = null.
    while (ipGr.next()) {
        // -&gt; each pass of the loop looks at one IP Address record.
        var owner = '' + ipGr.nic.cmdb_ci;
        // -&gt; owner = the sys_id of the CI at the end of this record's chain, for example
        //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c".
        if (!owners[owner]) {
            owners[owner] = true;
            count++;
            if (count == 1)
                first = owner;
        }
        // -&gt; the first time a CI is seen: owners gains it, count grows by one, and first remembers
        //    the very first CI.
    }
    // ---------------------------------------------------------------------------------------------
    // STAGE 7 - Decide: one owner that is not a load balancer, or decline
    // What happens : accepts the single owner when exactly one CI was collected and it is not a
    //                Load Balancer; declines otherwise.
    // Why          : two owners cannot be told apart by the address, and a load balancer answering
    //                on a virtual address is not the host that was scanned.
    // Sample       : count = 1 and isLoadBalancer("3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c") = false -&gt;
    //                return "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". count = 2, or the owner is a load
    //                balancer -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    if (count == 1 &amp;&amp; !isLoadBalancer(first))
        return first;
    // -&gt; exactly one owner and not a load balancer -&gt; its sys_id goes back to the framework.
    return null;
    // -&gt; no owner, several owners, or a load balancer -&gt; decline; the host continues to rule 850.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;IP&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:42:03&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;23ff4d7e936a4310e3aef0aefaba1072&lt;/sys_id&gt;&lt;sys_mod_count&gt;2&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:20&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>1083934253</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>d51fd5f2938b8710e3aef0aefaba10c0</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf3d10000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM IP Layered Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>b01fd5f29e8b8710fee5c9b607d1507f</update_guid>
<update_guid_history>b01fd5f29e8b8710fee5c9b607d1507f:1083934253,968a997e3c0b87108f3099178a618467:-804836697</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_816a0136932a4310e3aef0aefaba10bc</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;Unique short hostname inside the OS-implied class, for CIs without FQDN/domain data.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM Hostname Class Match&lt;/name&gt;&lt;order&gt;400&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;4&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 400 - USEM Hostname Class Match
   =================================================================================================
   PURPOSE
   Match by the short hostname alone, inside the class the OS implies. This is for CIs that carry
   only a short name and no domain information; the agreement between the CI class and the scanned
   OS replaces the missing domain evidence.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "35850078",
     "IP": "30.143.70.11",
     "TRACKING_METHOD": "IP",
     "OS": "Windows Server 2016 Standard 64 bit Edition Version 1607",
     "DNS": "wsaoi01zeapd1.sdi.corp.bankofamerica.com",
     "NETBIOS": "WSAOI01ZEAPD1"
   }

   sourceValue   = the DNS field of that record -&gt; "wsaoi01zeapd1.sdi.corp.bankofamerica.com"
   sourcePayload = the whole record above; the rule also reads the OS from it
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the Windows Server CI named "WSAOI01ZEAPD1" (names compare
   case-insensitively, so "wsaoi01zeapd1" finds it); a second Windows Server with the same name
   makes the rule decline.

   WHY THIS RULE SITS AT ORDER 400
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : 250 to 350 needed domain evidence on the CI (fqdn, dns_domain or the layered DNS
                 records).
   - Reaches it: hosts whose CI carries only a short name and no domain information at all.
   - After it  : 410 repeats the short name search across all hardware with a class sanity check;
                 450 handles CIs named with the full FQDN.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a DNS name
    2. Clean the DNS name and take the short hostname
    3. Read the list of CI classes that must never be matched
    4. Work out the CMDB class from the scanned operating system
    5. Search that class for the short name
    6. Decide: exactly one CI, or decline
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a DNS name
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "wsaoi01zeapd1.sdi.corp.bankofamerica.com" -&gt; not empty -&gt; the
    //                rule carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the DNS name and take the short hostname
    // What happens : '' + sourceValue turns the value into plain text, trim() removes blanks at
    //                both ends, toLowerCase() lowers it, and split(".")[0] keeps the text before
    //                the first dot.
    // Why          : CIs without domain information carry only the short name, so the short name is
    //                what has to be compared. A bare label with no dot works as well: split gives
    //                the whole text back.
    // Sample       : full = "wsaoi01zeapd1.sdi.corp.bankofamerica.com"; split(".") =
    //                ["wsaoi01zeapd1", "sdi", "corp", "bankofamerica", "com"]; host =
    //                "wsaoi01zeapd1".
    // ---------------------------------------------------------------------------------------------
    var full = ('' + sourceValue).trim().toLowerCase();
    // -&gt; full = "wsaoi01zeapd1.sdi.corp.bankofamerica.com".
    var host = full.split('.')[0];
    // -&gt; host = "wsaoi01zeapd1".
    if (!host)
        return null;
    // -&gt; host is empty only for a name that starts with a dot -&gt; decline; "wsaoi01zeapd1" -&gt; carry
    //    on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Work out the CMDB class from the scanned operating system
    // What happens : the helper classFor() reads the operating system text Qualys reports and
    //                returns the CMDB class such a machine is stored in. It lowers the text, gives
    //                up when the text is an unauthenticated multi-guess fingerprint (three or more
    //                guesses separated by "/"), then looks for key words in a fixed order and
    //                returns an empty string when nothing fits. Searching a class also searches
    //                every class beneath it, so cmdb_ci_linux_server covers all Linux flavour
    //                classes.
    // Why          : evidence is only trusted when it lands in a class that agrees with the scanned
    //                OS: a Red Hat host must resolve to a Linux Server CI, never to a Windows
    //                Server that happens to carry the same value.
    // Sample       : classFor("Windows Server 2016 Standard 64 bit Edition Version 1607"): s =
    //                "windows server 2016 standard 64 bit edition version 1607"; s.split("/") has 1
    //                part; "esx" is not found; "windows" is found and "server" is found too -&gt;
    //                returns "cmdb_ci_win_server" (Windows Server).
    // ---------------------------------------------------------------------------------------------
    function classFor(os) {
        if (!os) return '';
        // -&gt; no OS text at all -&gt; "" (no class).
        var s = ('' + os).toLowerCase();
        // -&gt; s = the OS text in lower case, so that "Windows", "WINDOWS" and "windows" all compare
        //    the same.
        if (s.split('/').length &gt; 2) return '';
        // -&gt; "ubuntu / tiny core linux / linux 2.6.x / ibm asm / hp storeonce / f5 networks big-ip
        //    / cisco ios software" splits into 7 parts at the "/" characters -&gt; more than 2 -&gt; an
        //    unauthenticated guess list -&gt; "" (no class).
        if (s.indexOf('esx') != -1) return 'cmdb_ci_esx_server';
        // -&gt; "vmware esxi 7.0.3 build 24723872" contains "esx" -&gt; "cmdb_ci_esx_server" (ESX
        //    Server).
        if (s.indexOf('windows') != -1)
            return s.indexOf('server') != -1 ? 'cmdb_ci_win_server' : 'cmdb_ci_computer';
        // -&gt; "windows server 2016 standard 64 bit edition version 1607" -&gt; "cmdb_ci_win_server"
        //    (Windows Server); "windows 10 enterprise 64 bit edition version 22h2" has no "server"
        //    -&gt; "cmdb_ci_computer" (Computer, a workstation).
        if (s.indexOf('aix') != -1) return 'cmdb_ci_aix_server';
        // -&gt; "aix 7.3 tl3" -&gt; "cmdb_ci_aix_server" (AIX Server).
        if (s.indexOf('solaris') != -1 || s.indexOf('sunos') != -1) return 'cmdb_ci_solaris_server';
        // -&gt; "oracle solaris 11.4" -&gt; "cmdb_ci_solaris_server" (Solaris Server).
        if (s.indexOf('hp-ux') != -1) return 'cmdb_ci_hpux_server';
        // -&gt; "hp-ux b.11.31" -&gt; "cmdb_ci_hpux_server" (HPUX Server).
        if (s.indexOf('netapp') != -1 || s.indexOf('ontap') != -1) return 'cmdb_ci_storage_server';
        // -&gt; "netapp ontap 9.12.1" -&gt; "cmdb_ci_storage_server" (Storage Server).
        if (s.indexOf('printer') != -1 || s.indexOf('laserjet') != -1 || s.indexOf('jetdirect') != -1) return 'cmdb_ci_printer';
        // -&gt; "hp laserjet m507" -&gt; "cmdb_ci_printer" (Printer).
        if (s.indexOf('red hat') != -1 || s.indexOf('linux') != -1 || s.indexOf('centos') != -1 ||
            s.indexOf('ubuntu') != -1 || s.indexOf('suse') != -1 || s.indexOf('debian') != -1 ||
            s.indexOf('fedora') != -1 || s.indexOf('euleros') != -1 ||
            s.indexOf('oracle enterprise') != -1 || s.indexOf('amazon') != -1) return 'cmdb_ci_linux_server';
        // -&gt; "red hat enterprise linux 9.8", "suse linux enterprise server 15 sp5" and "amazon
        //    linux 2023" -&gt; "cmdb_ci_linux_server" (Linux Server).
        if (s.indexOf('nx-os') != -1 || s.indexOf('catos') != -1 || s.indexOf('cisco') != -1) return 'cmdb_ci_netgear';
        // -&gt; "cisco nx-os 9.3(8)" -&gt; "cmdb_ci_netgear" (Network Gear).
        return '';
        // -&gt; anything else, for example "unknown" -&gt; "" (no class).
    }

    var pref = classFor(sourcePayload.OS);
    // -&gt; pref = "cmdb_ci_win_server" for the sample OS "Windows Server 2016 Standard 64 bit Edition
    //    Version 1607".
    if (!pref)
        return null;
    // -&gt; pref = "" happens for an unknown OS or a multi-guess fingerprint; this rule then declines
    //    so that rule 410 (which searches all hardware without a class) gets its turn.
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Search that class for the short name
    // What happens : opens a search on the class chosen above (and its sub-classes), keeps only CIs
    //                whose name equals the short hostname, and leaves out every class on the ignore
    //                list.
    // Why          : with no domain to confirm, the class is the only safeguard against a namesake;
    //                inside the agreed class the short name still has to be unique (next stage).
    // Sample       : the search on cmdb_ci_win_server for name = "wsaoi01zeapd1" finds the Windows
    //                Server "WSAOI01ZEAPD1" (sys_id 3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c).
    // ---------------------------------------------------------------------------------------------
    var gr = new GlideRecord(pref);
    // -&gt; gr = a search on the Windows Server class and every class beneath it.
    if (!gr.isValid())
        return null;
    // -&gt; the class does not exist on this instance -&gt; decline rather than fail.
    gr.addQuery('name', host);
    // -&gt; condition added: name = "wsaoi01zeapd1"; CMDB names compare case-insensitively, so
    //    "WSAOI01ZEAPD1" is found.
    if (ignore)
        gr.addQuery('sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
    //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    // ---------------------------------------------------------------------------------------------
    // STAGE 6 - Decide: exactly one CI, or decline
    // What happens : runs the search, reads the first CI and accepts it only when there is no
    //                second CI in the result.
    // Why          : every finding of this host will be linked to the CI the rule returns. A wrong
    //                CI sends the findings to the wrong owner, so when two CIs share the value the
    //                rule refuses to guess and answers null; a later rule with different evidence
    //                may still resolve the host, and if none does the host stays unmatched for the
    //                CMDB team to review.
    // Sample       : one CI -&gt; the Windows Server "WSAOI01ZEAPD1" -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". No CI -&gt; return null and rule 410 gets its
    //                turn. Two CIs -&gt; two Windows Servers named "WSAOI01ZEAPD1" (for example one
    //                retired and one rebuilt) -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    gr.query();
    // -&gt; the search has run; gr now sits just before the first row of the result (0, 1 or more
    //    CIs).
    if (!gr.next())
        return null;
    // -&gt; next() moves to the first CI and returns true. When the result is empty it returns false
    //    and the rule declines with null.
    var match = gr.getUniqueValue();
    // -&gt; match = the 32-character sys_id of that CI, for example
    //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"; this is the value the framework expects back.
    if (gr.hasNext())
        return null;
    // -&gt; hasNext() is true when a second CI is waiting in the result: two owners of one value is an
    //    ambiguity, so the rule declines.
    return match;
    // -&gt; exactly one CI: its sys_id goes back to the framework, which links the vulnerable item to
    //    it and stops evaluating later rules.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;DNS&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:17:35&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;816a0136932a4310e3aef0aefaba10bc&lt;/sys_id&gt;&lt;sys_mod_count&gt;3&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:20&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>-2012496299</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>d91fd5f2938b8710e3aef0aefaba10c1</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf21d0000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM Hostname Class Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>f01fd5f2c18b8710e43a39e716dfc242</update_guid>
<update_guid_history>f01fd5f2c18b8710e43a39e716dfc242:-2012496299,128a997eb70b87108012ffd62de7d54a:795608893</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
<sys_update_xml action="INSERT_OR_UPDATE">
<action>INSERT_OR_UPDATE</action>
<application display_value="Global">global</application>
<category>customer</category>
<comments/>
<name>sn_sec_cmn_ci_lookup_rule_a34a4df2932a4310e3aef0aefaba1089</name>
<payload>&lt;?xml version="1.0" encoding="UTF-8"?&gt;&lt;record_update sys_domain="global" table="sn_sec_cmn_ci_lookup_rule"&gt;&lt;sn_sec_cmn_ci_lookup_rule action="INSERT_OR_UPDATE"&gt;&lt;active&gt;true&lt;/active&gt;&lt;applies_to/&gt;&lt;condition/&gt;&lt;description&gt;CI named with the full FQDN string, hardware subtree only; unique owner required.&lt;/description&gt;&lt;lookup_script/&gt;&lt;lookup_target&gt;ci&lt;/lookup_target&gt;&lt;method&gt;script&lt;/method&gt;&lt;name&gt;USEM FQDN Name Hardware Match&lt;/name&gt;&lt;order&gt;450&lt;/order&gt;&lt;reapply&gt;true&lt;/reapply&gt;&lt;reapply_version&gt;4&lt;/reapply_version&gt;&lt;script&gt;&lt;![CDATA[/* =================================================================================================
   RULE 450 - USEM FQDN Name Hardware Match
   =================================================================================================
   PURPOSE
   Match CIs whose name field holds the complete FQDN string, in the hardware tree. Some CMDB loads
   (typically the appliance domains ending in .rpg) name the CI with the whole FQDN, so a short-name
   search can never find them.

   SAMPLE PAYLOAD
   One Qualys Host Detection record, used for every example in this script:
   {
     "ID": "71973166",
     "IP": "164.91.209.12",
     "TRACKING_METHOD": "AGENT",
     "OS": "Red Hat Enterprise Linux 8.10",
     "DNS": "lva40bneehcs01.ecomm.devicenp.rpg",
     "QG_HOSTID": "633781ed-019b-0002-2f2f-0050569d20ff"
   }

   sourceValue   = the DNS field of that record -&gt; "lva40bneehcs01.ecomm.devicenp.rpg"
   sourcePayload = the whole record above
   rule          = this lookup rule record (name, order, source); the logic does not need it
   Expected outcome for the sample: the hardware CI whose name is exactly
   "lva40bneehcs01.ecomm.devicenp.rpg"; its sys_id is returned when it is the only one.

   WHY THIS RULE SITS AT ORDER 450
   Rules run from the lowest order to the highest. The first rule that returns a CI wins and every
   later rule is skipped; a rule that returns null simply passes the host on to the next rule.
   - Before it : every rule so far compared the short hostname or the fqdn field.
   - Reaches it: CIs named with the full FQDN, which none of the earlier rules can see.
   - After it  : 700 and above (IP address rules) and finally 850, the only rule allowed to search
                 the broad cmdb_ci table by name.

   THE STAGES OF THIS SCRIPT
    1. Check that Qualys sent a DNS name
    2. Clean the DNS name and require a domain part
    3. Read the list of CI classes that must never be matched
    4. Search the whole hardware tree for a CI named with the full FQDN
    5. Decide: exactly one CI, or decline
   ================================================================================================= */
(function process(rule, sourceValue, sourcePayload) {
    // ---------------------------------------------------------------------------------------------
    // STAGE 1 - Check that Qualys sent a DNS name
    // What happens : the rule stops with null when the field is empty. null is the signal "no match
    //                from this rule"; the framework then tries the next rule in order.
    // Why          : a search for an empty value can never identify one machine and would only cost
    //                time on every host that lacks the field.
    // Sample       : sourceValue = "lva40bneehcs01.ecomm.devicenp.rpg" -&gt; not empty -&gt; the rule
    //                carries on.
    // ---------------------------------------------------------------------------------------------
    if (!sourceValue)
        return null;
    // -&gt; for the sample the condition is false and nothing happens; for an empty value the rule
    //    ends here with null.
    // ---------------------------------------------------------------------------------------------
    // STAGE 2 - Clean the DNS name and require a domain part
    // What happens : '' + sourceValue turns the value into plain text, trim() removes blanks at
    //                both ends and toLowerCase() lowers it. Then the rule requires at least one dot
    //                in the name.
    // Why          : DNS names are case-insensitive and CMDB values are stored in mixed case, so
    //                both sides are compared in lower case. A name without a dot is a bare
    //                hostname, which the hostname rules (400 and 410) look after.
    // Sample       : fqdn = "lva40bneehcs01.ecomm.devicenp.rpg"; indexOf(".") finds a dot -&gt; carry
    //                on. A bare label "lva40bneehcs01" has no dot -&gt; indexOf(".") = -1 -&gt; decline.
    // ---------------------------------------------------------------------------------------------
    var fqdn = ('' + sourceValue).trim().toLowerCase();
    // -&gt; fqdn = "lva40bneehcs01.ecomm.devicenp.rpg".
    if (fqdn.indexOf('.') == -1)
        return null;
    // -&gt; indexOf(".") = 14 for the sample (the position of the first dot) -&gt; not -1 -&gt; carry on.
    // ---------------------------------------------------------------------------------------------
    // STAGE 3 - Read the list of CI classes that must never be matched
    // What happens : reads the list of CI classes that must never be matched. Administrators keep
    //                it in the system property sn_sec_cmn.ignoreCIClass as comma separated class
    //                names. The framework may also hand the same list to the script as a variable
    //                called _ignoreClass; when that variable exists and is filled the script uses
    //                it, otherwise it reads the property directly.
    // Why          : placeholder and technical classes must never receive vulnerability findings:
    //                the unmatched CI placeholders that Security Operations creates, the Qualys
    //                staging CI class, Unclassed Hardware, incomplete IP records and DNS Name
    //                records. Keeping the list in one property means it can be changed without
    //                editing sixteen scripts.
    // Sample       : with the platform default the property holds
    //                "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name";
    //                an empty property gives ignore = "" and then no class filter is added to the
    //                searches below.
    // ---------------------------------------------------------------------------------------------
    var ignore = (typeof _ignoreClass != 'undefined' &amp;&amp; _ignoreClass) ?
        ('' + _ignoreClass) : gs.getProperty('sn_sec_cmn.ignoreCIClass', '');
    // -&gt; ignore =
    //    "sn_sec_cmn_unmatched_ci,sn_vul_qualys_ci,cmdb_ci_unclassed_hardware,cmdb_ci_incomplete_ip,cmdb_ci_dns_name",
    //    one text, comma separated, ready for a NOT IN condition.
    // ---------------------------------------------------------------------------------------------
    // STAGE 4 - Search the whole hardware tree for a CI named with the full FQDN
    // What happens : opens a search on cmdb_ci_hardware, keeps only CIs whose name equals the
    //                complete lower-cased DNS name, and leaves out every class on the ignore list.
    // Why          : the name field is compared with the whole string including the domain, which
    //                is exactly what the earlier rules never did.
    // Sample       : the search on cmdb_ci_hardware for name = "lva40bneehcs01.ecomm.devicenp.rpg"
    //                finds the Server "lva40bneehcs01.ecomm.devicenp.rpg" (sys_id
    //                3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c).
    // ---------------------------------------------------------------------------------------------
    var gr = new GlideRecord('cmdb_ci_hardware');
    // -&gt; gr = a search on Hardware and every class beneath it.
    gr.addQuery('name', fqdn);
    // -&gt; condition added: name = "lva40bneehcs01.ecomm.devicenp.rpg" (the full string,
    //    case-insensitive).
    if (ignore)
        gr.addQuery('sys_class_name', 'NOT IN', ignore);
    // -&gt; condition added: sys_class_name NOT IN (sn_sec_cmn_unmatched_ci, sn_vul_qualys_ci,
    //    cmdb_ci_unclassed_hardware, cmdb_ci_incomplete_ip, cmdb_ci_dns_name).
    // ---------------------------------------------------------------------------------------------
    // STAGE 5 - Decide: exactly one CI, or decline
    // What happens : runs the search, reads the first CI and accepts it only when there is no
    //                second CI in the result.
    // Why          : every finding of this host will be linked to the CI the rule returns. A wrong
    //                CI sends the findings to the wrong owner, so when two CIs share the value the
    //                rule refuses to guess and answers null; a later rule with different evidence
    //                may still resolve the host, and if none does the host stays unmatched for the
    //                CMDB team to review.
    // Sample       : one CI -&gt; the Server "lva40bneehcs01.ecomm.devicenp.rpg" -&gt; return
    //                "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c". No CI -&gt; return null and the host
    //                continues to the IP rules (700 and above). Two CIs -&gt; two hardware CIs with
    //                that full name -&gt; return null.
    // ---------------------------------------------------------------------------------------------
    gr.query();
    // -&gt; the search has run; gr now sits just before the first row of the result (0, 1 or more
    //    CIs).
    if (!gr.next())
        return null;
    // -&gt; next() moves to the first CI and returns true. When the result is empty it returns false
    //    and the rule declines with null.
    var match = gr.getUniqueValue();
    // -&gt; match = the 32-character sys_id of that CI, for example
    //    "3f2a9c7e1b8d4a5f9e6c0d2b7a4f8e1c"; this is the value the framework expects back.
    if (gr.hasNext())
        return null;
    // -&gt; hasNext() is true when a second CI is waiting in the result: two owners of one value is an
    //    ambiguity, so the rule declines.
    return match;
    // -&gt; exactly one CI: its sys_id goes back to the framework, which links the vulnerable item to
    //    it and stops evaluating later rules.
})(rule, sourceValue, sourcePayload);
]]&gt;&lt;/script&gt;&lt;source display_value="Qualys Cloud Platform"&gt;ed44bdc453220300e8f9f745911c0801&lt;/source&gt;&lt;source_field&gt;DNS&lt;/source_field&gt;&lt;sys_created_by&gt;admin&lt;/sys_created_by&gt;&lt;sys_created_on&gt;2026-08-10 14:17:12&lt;/sys_created_on&gt;&lt;sys_domain&gt;global&lt;/sys_domain&gt;&lt;sys_domain_path&gt;/&lt;/sys_domain_path&gt;&lt;sys_id&gt;a34a4df2932a4310e3aef0aefaba1089&lt;/sys_id&gt;&lt;sys_mod_count&gt;3&lt;/sys_mod_count&gt;&lt;sys_updated_by&gt;admin&lt;/sys_updated_by&gt;&lt;sys_updated_on&gt;2026-09-04 12:19:20&lt;/sys_updated_on&gt;&lt;table&gt;sn_vul_qualys_host_attrb&lt;/table&gt;&lt;target_field/&gt;&lt;target_field_product_model/&gt;&lt;target_table/&gt;&lt;target_table_product_model&gt;cmdb_application_product_model&lt;/target_table_product_model&gt;&lt;type&gt;custom&lt;/type&gt;&lt;/sn_sec_cmn_ci_lookup_rule&gt;&lt;/record_update&gt;</payload>
<payload_hash>579044174</payload_hash>
<remote_update_set display_value="SNOWUSEMTP-895_MS_Qualys CI Lookup Rules_V2.1">191fd5f2938b8710e3aef0aefaba10bf</remote_update_set>
<replace_on_upgrade>false</replace_on_upgrade>
<sys_created_by>admin</sys_created_by>
<sys_created_on>2026-09-04 12:19:22</sys_created_on>
<sys_id>dd1fd5f2938b8710e3aef0aefaba10c2</sys_id>
<sys_mod_count>0</sys_mod_count>
<sys_recorded_at>1a06c5bf2b60000001</sys_recorded_at>
<sys_updated_by>admin</sys_updated_by>
<sys_updated_on>2026-09-04 12:19:22</sys_updated_on>
<table>sn_vul_qualys_host_attrb</table>
<target_name>USEM FQDN Name Hardware Match</target_name>
<type>Lookup Rule</type>
<update_domain>global</update_domain>
<update_guid>701fd5f20c8b871095f1d626f5d89f4a</update_guid>
<update_guid_history>701fd5f20c8b871095f1d626f5d89f4a:579044174,9a8a997eb20b87108567aad8d504934f:-278054573</update_guid_history>
<update_set display_value=""/>
<view/>
</sys_update_xml>
</unload>
